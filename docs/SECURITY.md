# SECURITY.md

Living security tracker: open findings, resolved findings, and the controls
relied on. Read it before changing anything that touches git operations,
subprocess invocation, the publisher, or an agent's write scope.

Every finding carries four things, or it rots into an untested claim:
`file:line`, a severity, a one-line verification check, and a suggested fix.

## Verified strengths

- Git operations run through `GitGateway` behind a policy; force pushes and
  destructive commands are denied rather than merely discouraged.
- `protected_branches` refuses direct pushes to the base branch.
- The reviewer runs under a sandbox policy that is preflighted before a round:
  `codex.sandbox_args` ships `--sandbox read-only`, and `codex_cli` refuses to
  launch — runner and `doctor` alike — when it names no enforceable mode. Its
  prompt is self-contained, so it is never asked to read the checkout. NOT
  claimed: that it cannot. A read-only sandbox permits reads, enforcement is
  codex's own, and the working directory (outside the checkout) confines
  nothing — `cwd` chooses where a process starts.
- The escape detector brackets every write-capable agent call.

## Open findings

_None recorded yet in this repository._

## Change notes — append ONE new line at the END of this file

<!-- CHANGE-NOTES: append below, one line per note, at the END of the file. -->

| Date | Task | Note |
|---|---|---|
| 2026-08-27 | split | extracted from language-app with git filter-repo, 414 commits of history preserved; this tracker seeded here. |
| 2026-08-31 | brw-19c | `autoloop/cli.py:_precondition_browser` — a name-whitelist recheck is disarmed by deleting the checks it names, and nothing fails while it happens. Severity: medium; a blocker recording an unresolved ENVIRONMENT fault becomes resolvable by answer text alone, which is Autoloop M1 finding #7's exact shape. Verify: answering a `login_expired` blocker must be REFUSED while the configured provider cannot be opened. Fixed here — the whitelist is `_BROWSER_PRECONDITION_CHECKS`, led by `primary_live`, which `run_doctor` still emits; `cdp`/`playwright` are retained but dead. |
| 2026-08-31 | brw-19d | `autoloop/cli.py:3365` `_precondition_transport_live` — a name-filter recheck that reads ABSENT evidence as a pass. Severity: medium; `login_expired`/`submission_ambiguous` become resolvable by answer text alone once doctor stops emitting the filtered rows. Verify: stub `doctor.run_doctor` to return no `primary_live` and no `provider` row, then answer such a blocker — it must be REFUSED and stay open. Fixed: required rows must be PRESENT and `ok`, a sweep that raises refuses, and `autoloop/cli.py:3417` `_precondition_git_health` takes `git_failure_budget_exhausted` off transport evidence entirely. |
| 2026-08-31 | notify-01 | `autoloop/notify.py:397` `_resolve_credentials` — NEW OUTBOUND SURFACE: the loop authenticates to an SMTP server and mails its status from inside `heartbeat.publish`. Severity: medium — falling back to an unauthenticated send would put status text wherever that port answers, and the credential is the operator's mail account. Verify: with `[notify].enabled = true` and the variable named by `password_env` unset or empty, or `password_file` missing or blank, the send must be REFUSED with the reason on stderr and nothing sent. Pinned by `test_notify.py`'s seven no-password shapes. |
| 2026-08-31 | notify-01 | Fix for the surface above: no config key holds the password (`config.py:_load_notify_section` refuses `notify.password`/`smtp_password` by name, names rotation, never echoes the value), exactly one of `password_env`/`password_file` is required when enabled, `tls = "none"` is refused so it is never sent in clear, and every rendered or logged string passes one choke point, `autoloop/notify.py:199` `_safe`, which replaces the password with `***` and rewrites the operator's home to `~`. NOT enforced, check by hand: `password_file` outside the checkout, `chmod 600`. |
| 2026-09-01 | wanted-01 | `autoloop/policy.py:468` `_check_wanted_decision` — a NEW field required on every directive, and why it is not a new authority surface. Severity: informational. The gate tests PRESENCE only: it never compares the value to `Decision`, never restricts it to a vocabulary, and `orchestrator._dispatch` still branches on `decision` alone — so a reviewer writing `wanted_decision: "push"` gets it COUNTED, not executed, leaving finding #2's circular-ownership bound exactly where it was. Verify: a `stop` naming any real verb leaves the candidate, the registry and the worker repo untouched. No fix needed; pinned by `test_wanted_decision.py`. |
| 2026-09-01 | prov-01 | `autoloop/codex/conversation.py:372` `submit` — the verdict was read from the WHOLE of `codex exec` stdout, which carries the ECHOED PROMPT under a `user` role marker. Severity: medium. The prompt carries the response contract and, in the captured shape, an example directive, so text the loop SENT could be read back as the approval that authorizes a commit or a push — `quota.py`'s echo hazard, on the other stream. Verify: stdout whose `user` segment holds a complete `commit_and_push` and whose `codex` segment holds a `push` must yield `push`. Fixed: `codex/reply.py` isolates the `codex` segment; pinned by `test_codex_stdout_verdict.py`. |
| 2026-09-01 | prov-01 | `autoloop/codex/reply.py:277` `_turn_markers` — isolating "the segment after the `codex` marker" is not on its own enough: this loop maintains itself, so its packets QUOTE codex transcripts and a marker plus a complete directive arrive inside the echoed prompt. Severity: medium; an echoed example carries this round's real `request_id` and `head_sha`, so `orchestrator`'s stamp gates would accept it. Verify: stdout whose only `codex` marker sits inside the echoed prompt must be REJECTED, never parsed. Fixed: markers must be at column 0 AND at a turn boundary, and `reply.py:375` `_anchor` skips a verbatim echo outright. |
| 2026-09-01 | prov-01 | `autoloop/codex/reply.py:517` `isolate_reply` — the bound above holds only while the echo is VERBATIM: a build that re-wraps its echo defeats the exact match, leaving the packet's own quoted directive as the single segment. Severity: medium; it carries this round's real `request_id` and `head_sha`, so the stamp gates accept it and an approval nobody made authorizes a push. Verify: a reflowed echo quoting a flush-left same-round directive, with no reviewer message, must be REJECTED. Fixed: `reply.py:474` `_accept` refuses any candidate the prompt contains, literally or on `quota._squeeze`'s basis. |
| 2026-09-01 | prov-02 | `autoloop/codex/preflight.py:1` — the reviewer's confinement is now STATED rather than implied: `codex.sandbox_args` is empty by policy and containment rests on `working_dir` alone (a dedicated empty directory outside the checkout, plus a self-contained prompt). Severity: informational; nothing is weaker than before — the previous default ran in the HOME directory, which is strictly more exposed than one empty directory — and the older "flag names cannot be verified" note had expired into an implied sandbox nobody enforced. Verify: `doctor`'s `codex_sandbox` row must say `warn` and name the policy, and `codex_workdir` must `fail` for any path inside the checkout. |
| 2026-09-01 | prov-02 | `autoloop/doctor.py:13d` — NEW LOCAL SIDE EFFECT: the preflight LAUNCHES `codex exec` with a fixed one-line prompt and creates `~/.autoloop/codex-workdir` if absent. Severity: low. No review packet, no repository content and no credential leaves the machine that codex is not already signed into; the prompt is a module constant, the deadline is its own (120s, not `codex.timeout_seconds`), and output reaching a row is bounded and echo-stripped by `quota.failure_digest`. Verify: `DoctorProbes.codex_preflight` must be stubbed in every test that runs a sweep, and no test may spawn the binary. |
| 2026-09-01 | prov-02 | SUPERSEDES the prov-02 note above and the `Verified strengths` bullet, both corrected: `autoloop/codex/conversation.py:55` claimed the reviewer got no repository access because it ran in a dedicated directory outside the checkout. Severity: medium — a FALSE containment claim, which is worse than a stated gap. `cwd` selects where a process starts and restricts no absolute path, no `..`, no subprocess and no read, so the seat was UNCONFINED while `doctor` reported the empty `codex.sandbox_args` as a deliberate policy. Verify: `codex.sandbox_args = []` must FAIL `doctor` and raise before any process starts. |
| 2026-09-01 | prov-02 | Fix for the above: `codex.sandbox_args` ships `["--sandbox", "read-only"]`, `autoloop/codex/sandbox.py:185` `describe_sandbox` grades it fail-closed, and `conversation.py:196` `run`, `preflight.py:280` `preflight_codex` and `doctor.py:511` all refuse an unenforceable one. CLAIMED: the flags are present in the reviewer's real argv, and the preflight proves the configured build accepts them. NOT claimed: that a sandbox is enforced (no codex binary runs here or in CI) or that reads are confined — read-only permits them. `codex_app_server` gets no policy; it answers every approval `abort`. |
| 2026-09-01 | prov-02 | `autoloop/codex/sandbox.py:208` and the same sentence in `conversation.py`, `config.py`, `config.example.toml` and `docs/AUTOLOOP.md` said `--sandbox read-only` refuses WRITES AND COMMAND EXECUTION. It does not: codex runs commands INSIDE the read-only sandbox, restricting what they write. Severity: low — a claim defect, not a code one; no grading, status or refusal changed. Fixed here, and narrowed to what is verifiable: writes restricted, reads NOT confined, commands NOT refused, network behaviour unverified here. Verify: the `codex_sandbox` row must say `commands still run`; pinned for all four read-only spellings by `test_codex_preflight.py`. |
| 2026-09-01 | prov-02 | `autoloop/codex/preflight.py:237` `ensure_working_dir` — narrows the local side effect noted above: `~/.autoloop/codex-workdir` is created ONLY when `codex.working_dir` is empty. Severity: low. Provenance was inferred by path equality, so a config naming that path EXPLICITLY was created for the operator instead of refused, widening "autoloop creates only its own directory" to cover a path someone else may have meant. Verify: with `codex.working_dir` set to the default path while absent, `codex_workdir` must `fail` and the directory must still not exist. Fixed: provenance travels from the setting, and the parameter defaults to refusing. |
| 2026-09-08 | ctx-04 | Extends the inbox finding class ("a request can rewrite an existing task's `approved_paths`", S28/S30) to `autoloop/inbox.py:330` `KIND_CONTEXT_IDS` — a new request kind with the SAME shape, refused on an in-flight task by the same `TaskRegistry._refuse_immutable`. Severity: informational, and the reason it is not a second finding: it writes `Task.context_ids`, which neither `tasks.unauthorized_paths` nor `tasks.effective_approved_paths` reads, so it moves no authorization. Verify: attach any ids and `effective_approved_paths` must be byte-identical; pinned by `test_tasks.py`'s two named acceptance tests. |
| 2026-09-09 | ctx-04 | Strengthens the verification on the line above rather than restating it: that byte-identical check hands `effective_approved_paths` a path tuple itself, so it cannot fail on a leak wired in at a CALL SITE. Verify additionally: `test_tasks.py::test_no_scope_decision_in_the_package_is_handed_a_context_reference` parses every call to `effective_approved_paths`/`unauthorized_paths`/`deletable_paths`/`authorized_cleanup_paths` in the package and refuses any argument that reads `context_ids`, covering `orchestrator.py`'s five calls and `implement_executor.py`'s four, which no behavioural test of this field reaches. |
| 2026-09-09 | ctx-05 | S33 (context is data, not instruction) gains a THIRD text source: `autoloop/context_packet.py:1` renders record titles, invariants and source paths — written outside this package — into the agent prompt and into the review packet. Severity: informational; both of S33's controls are unchanged and neither is sanitisation. ORDERING: the section is rendered after every stamp line in both, so a first-match read still lands on the real value. VERIFICATION: `contract.verify_review` refuses a copied echo. Verify: a record whose title carries `report_sha256: 000…` must not be readable as the stamp, and must still appear unedited; pinned by `test_context_packet.py`. |
| 2026-09-09 | ctx-05 | Second half of the surface above, the echo direction: nothing computes with a context packet. It is never parsed, no gate reads one, `tasks.effective_approved_paths` is never handed a record, and every foreign string is collapsed to one line behind a prefix — so a record cannot open a line beginning `DELETE-FILE:` or `ASSUMPTION:` that an agent quoting its prompt would forge into a request the executor acts on. Verify: `test_context_packet.py::test_no_line_of_a_packet_can_be_opened_by_a_record`, over a title, an invariant and a source path each carrying an embedded newline. |
| 2026-09-09 | ctx-05 | Closes the ECHO half of the surface above: the digest was recorded and shown to the reviewer while the prompt carried no packet at all — the `context_packet_for` keyword was wired by nothing, and production's executor is `cli._DispatchingExecutor`, which forwards only `execute`, so no keyword or attribute could reach the object building the prompt. The dispatch now hands the render over by name (`implement_executor.deliver_round_context_packet`). Verify: `test_context_packet.py` §10 drives a real `ImplementExecutor` and finds the EXECUTION RECORD's digest in the agent's own prompt. |
| 2026-09-09 | ctx-05 | Fail-closed half of the line above: a digest naming a packet nobody can produce is evidence of context that was never evidenced, so a round whose packet cannot be stored AND read back at the digest its record carries never starts — no agent, no attempt charged, `context_packet_unavailable` (task_fatal; loop_fatal would need an entry in `blockers.LANE_FATAL_CODES`/`FLEET_FATAL_CODES`, out of scope here) naming the directory that failed. Verify: with `os.replace` raising, and separately with the stored bytes corrupted after a successful write, the agent must record zero prompts. |
| 2026-09-09 | ctx-06 | `autoloop/audit/taskgen.py` `generate_tasks` — a proposed task could assert a repository fact no reader produced, and generation could silently prefer one of two disagreeing sources. Severity: medium; an uncited "current behaviour" sends a round to fix something that may not exist. Verify: a finding whose `evidence` names no `path:line`, or whose `current_behaviour` carries no citation, must produce NO task and must name the claim in `proposal.skipped`; a scope conflict must leave `tasks` empty and a `planning_source_conflict` record naming both sides. Fixed by `inbox.unsupported_claims` / `detect_conflicts`. |
| 2026-09-09 | ctx-06 | `autoloop/audit/taskgen.py` `_record_conflicts` — the fail-open beside the finding above: a conflict whose durable record could NOT be written was admitted in `record_notes`, which nothing renders, and generation continued regardless. Severity: medium. It now STOPS, and the reason reaches `proposal.skipped`, the one proposal field `audit/report.py` prints. Verify: with `blocker_store=None`, a SCOPE_UNCHANGED conflict must leave `tasks` empty and put `NO DURABLE RECORD EXISTS` in the rendered report. Not fixed here: `audit/executor.py:803` passes no store at all, and that file is outside ctx-06's approved paths. |
| 2026-09-09 | ctx-06 | `autoloop/inbox.py:2000` `claim_problem` — round 1 accepted any path-SHAPED string cited to the audit report, so an agent could support a repository claim with `autoloop/nowhere.py:12` it never opened, and a task would be filed to fix something that may not exist. Severity: medium. Verify: a finding whose `evidence` cites a path absent from `git ls-files` must produce NO task and must NAME the path; with no tree reader the same finding must be refused rather than believed. Fixed by `inbox.TreeReader` and the check in `claim_problem`; `cli._planning_sources` supplies the reader in production. |
| 2026-09-09 | ctx-06 | `autoloop/audit/taskgen.py` `_description` — beside the finding above: a `sources` claim bypassed the citation rule entirely and its sentence was rendered into the generated task, where a fresh session cannot tell a quoted source from a verified fact. Severity: medium. Verify: with an uncited source claim, the task description must NOT contain that sentence, must say the assertion was withheld, and the durable blocker must still carry it verbatim. Fixed by `inbox.describe_conflict_for_task`; the claim is still compared and recorded, since dropping it would hide the disagreement. |
| 2026-09-09 | ops-01 | `autoloop/dashboard.py` `Handler._operator_control` — a THIRD write path on an unauthenticated localhost page, and the widest: it can stop the loop, retire a task and merge a backlog. Severity: medium, and unchanged in KIND from `/api/task` (S28): same 127.0.0.1 bind, same `X-Autoloop` header and Origin check, closed field vocabulary, and every effect is a fixed CLI verb in a subprocess rather than a write from this process. Verify: with `_ACTION_LOCK` held a POST must answer 409 having run nothing; without the header, 403. NOT fixed: the page still has no authentication. |
| 2026-09-09 | ops-01 | `autoloop/dashboard.py` `_control_argv` — operator free text reaches a CLI as argv, and a `--reason` or an answer beginning with `-` would be read by argparse as another option. Severity: low; the verb list is fixed, so nothing can inject a subcommand, but a value read as a flag is a command nobody typed. Verify: `_control_argv` for `answer` with the text `--help` must parse back through `cli.build_parser()` with `text == "--help"`. Fixed — every flag is emitted `--flag=value` and every positional sits after a bare `--`. |
| 2026-09-09 | ops-01 | `autoloop/dashboard.py` `perform_control` — the fail-OPEN this closes: a control decided only from the phase at CLICK time. `pause` exits at the top of `Orchestrator.run` WITHOUT a phase gate, so a stop armed in `executing` can land in `delivering`, where a packet's numbered parts are already deposited. Severity: medium — acting there strands work an approval may already cover. Verify: with the loop moving to `delivering` before it drops the lock, the verb must not run and the loop must be restarted. Fixed — the phase is re-read after the lock is released. |
| 2026-09-09 | ops-01 | `autoloop/dashboard.py:8076` `perform_control` — the same fail-OPEN one level down: the post-boundary re-read asked lane 0 alone, so a fleet that PASSED the pre-check could land with lane 1 mid-deposit and the verb ran. Severity: medium; reachable wherever `state_dir/lanes/` has an occupant. Verify: with lane 0 landing in `ready` and `lanes/_lane-1` in `delivering`, the verb must not run, the refusal must name `_lane-1`, and the loop must be restarted. Fixed — the re-read walks `lanes/` exactly as the pre-check does, so an unlistable one refuses too. |
| 2026-09-10 | ops-01 | `autoloop/dashboard.py:8125` `perform_control` — that fail-OPEN a level further down: the rollback fired only `if unsafe and action.verb`, so `pause` and `abort`, which have no verb, were left DOWN in `delivering` and reported as a stop that worked. Severity: medium — the packet is stranded and the report denies it. Verify: Pause armed in `executing` and landing in `delivering` must answer 409, spawn `run --continuous` and report `loop.restarted` true. Fixed — every action that armed a stop is rolled back; `restarts` governs the success path only. |
| 2026-09-10 | ops-01 | `autoloop/dashboard.py:8266` `_restart` — the last fail-OPEN in that sequence: the restart was verified against a live LOCK and a re-read of the PAUSE flag only, though the same function clears TWO. An abort flag armed between the clear and the lock read was reported as "restarted and verified alive, pid N" about a loop `cli._run_continuous` ends at its next outer iteration. Severity: low — it leaves the loop down quietly, which is the outcome the panel exists to make loud. Verify: a spawn that takes the lock and touches either flag must report `restarted` false and name the flag. Fixed by `_armed_stop_flag`. |
