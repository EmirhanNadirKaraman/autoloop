# TESTS.md

What is and is not covered, plus any known pre-existing failures.

Run the suite with `python3 -m pytest autoloop/tests`. `pytest.ini` supplies
`-p no:randomly` and `-m "not isolated"`; both are load-bearing and explained
there.

| Area | Where |
|---|---|
| Loop phases, dispatch, review | `autoloop/tests/test_orchestrator.py` |
| Task registry and graph | `autoloop/tests/test_tasks.py` |
| Test selection | `autoloop/tests/test_test_selection.py` |
| Change-note merging | `autoloop/tests/test_docs_merge.py` |
| Codex transports | `autoloop/tests/test_codex_provider.py`, `test_codex_app_server.py` |
| Context records and their stamp | `autoloop/tests/test_context_records.py` |

**Known failing on a fresh split (2026-08-27):** roughly 19 test files resolve
`Path(__file__).resolve().parents[2]` and assert about the repository that
contains them. They were written against the parent repository and need
fixture repositories instead; `test_audit_charters.py` already shows the shape.

## Change notes — append ONE new line at the END of this file

<!-- CHANGE-NOTES: append below, one line per note, at the END of the file. -->

| Date | Task | Note |
|---|---|---|
| 2026-08-27 | split | extracted from language-app with git filter-repo, 414 commits of history preserved; this tracker seeded here. |
| 2026-08-27 | select-02 | new `autoloop/tests/test_prose_doc_selection.py` pins the prose-document carve-out in test selection: `test_docs_merge.py` still runs on a docs-only round, `docs/audit_charters.toml` selects exactly what the untouched reference-token rule selects, a newly added doc-reading test is picked up with no list edited, and a document nothing reads still widens. `test_test_selection.py`'s "every test naming a tracker runs on a tracker change" claim is reversed there and corrected in place. |
| 2026-08-27 | select-03 | `test_prose_doc_selection.py` gains two cases: a glob of only wildcards names no document while `*.md` and `docs/AUDIT_*.md` still do; and `autoloop/dashboard.py` is not a reader of the trackers it never opens — asserted against the SHIPPED module rather than a fixture, because the defect was a property of the real source and a fixture would have passed throughout. Both fail with the fix reverted. |
| 2026-08-28 | perf-01 | `test_prose_doc_selection.py`'s `test_every_tracker_has_a_reader_so_a_change_note_round_never_widens` is parametrised over `TRACKER_PATHS` rather than looping inside one test: each tracker costs a full real-repository selection, and six in series made it the slowest test in the suite at 104s under `-n auto`, all on one worker while the rest idled. A cached `real_readers()` answers the reader question once for the file, which also drops a second `build_import_graph` from `test_dashboard_is_not_a_reader_of_the_trackers_it_never_opens`. Whole suite on 8 cores, all of perf-01 measured back to back against an otherwise idle machine: 446.37s -> 418.18s. |
| 2026-08-28 | perf-01 | New `autoloop/tests/gitrepo.py` builds a test repository by copying a per-process template instead of running `init`, three `config`, `add` and `commit` each time. Stdlib only, so it adds no edge to the graph the selector reads, and the cache lives under a directory the PROCESS made, so xdist workers neither share nor race on it. 30 files whose builder matched that shape byte-for-byte were converted mechanically, carrying through each one's branch, file, content, identity and message — author and message are part of the commit object. |
| 2026-08-28 | perf-01 | Which builders to convert was decided by measurement: `PYTEST_CURRENT_TEST` attributed all 2,206 `git init` calls to their tests, and those 30 files cover 1,461. Per run 2,206 -> 1,218. A shared initial sha is not a new hazard — one-second commit granularity already made independent builds byte-identical, and a branch is a ref that never enters the commit. On 8 cores this is lost in noise (the suite sits at 606% of 800%, so it is not CPU-bound); at `-n 4`, the CI shape and 88% utilisation, 545.73s -> 509.83s. |
| 2026-08-28 | perf-01 | `scripts/check_selection_soundness.py` runs the suite under per-test coverage contexts and asserts each test's statically derived module set, closed over the import graph, COVERS what it actually executed. Coverage is the check, never the mechanism: a recorded map is stale the moment code moves and its failure mode is silent under-selection. Its own CI job (`selection-soundness`), parallel to the test job so it adds no wall time. Contexts survive `-n auto`; serially it takes over an hour. |
| 2026-08-28 | perf-01 | That gate found four defects the static analysis reported as fine, each a silent under-selection: sibling test-module imports (`autoloop/tests` is on sys.path, no `__init__.py`), classes that build wiring in `__init__`, autouse fixtures no test's text points at, and the check omitting the forward import closure. 105 violations -> 0 over 3,865 tests. `test_per_test_deps.py` pins each as a fixture-only case. An earlier 276-test sample scored 0 and proved nothing — it contained none of those shapes, so the gate must run over the WHOLE suite. |
| 2026-08-28 | perf-01 | `test_per_test_selection.py` pins what the `per_test` mode REFUSES to drop, which is where its safety lives: the file set equals `reachable`'s as an equality, a changed test file keeps every test, a prose round drops nothing, an unreadable drop-list drops nothing, and the list sits OUTSIDE the checkout because validation runs in the worker repo and the gate after it refuses a dirtied tree. Two cases drive a real pytest run to prove the plugin drops a function with all its parametrised cases, and is inert with no list named. |
| 2026-08-28 | perf-01 | `test_audit_executor.py` gains two cases: an audit unit id is outside the roadmap namespace while both spellings stay recognised, and audit validation is normalised like every other validation run. `test_orchestrator.py` and `test_m1_hardening.py` now read the prefix off `contract.AUDIT_UNIT_PREFIX` instead of spelling `audit-` themselves, so the next rename moves one constant. |
| 2026-08-28 | perf-01 | `test_per_test_selection.py`'s two nested-pytest cases now run the child ISOLATED: `-p no:randomly` passed explicitly, `PYTEST_*` stripped from its environment, and `cwd` the target with the package on `PYTHONPATH`. Handing pytest a path outside the checkout moves ROOTDIR to that path, so `pytest.ini` is never read and its `-p no:randomly` stops applying — the child then seeded numpy from the inherited xdist environment and numpy refused it (`Seed must be between 0 and 2**32 - 1`). Passed solo and in a full local run; failed only inside the loop's parallel validation, where it refused a real round. |
| 2026-08-28 | port-04 | New `test_project_status.py` builds REAL deployments on disk — a checkout with a `.py` file in it, a state dir, a config file — and pins the multi-project view: an unreadable config is one `unknown` row while the others render, a live lock plus an open task_fatal blocker is never `stopped` (asserted on the RENDERED line, since a reader conflating them was the 2026-08-15 false alarm), and observing writes NOTHING, proven by a byte-for-byte snapshot of every tree with a real lock held and the real `health.check` running. Also: a stale lock still reports its open blockers, and unreadable counts render `—` rather than `0`. |
| 2026-08-30 | upgrade-01 | `test_self_upgrade.py` gains a section per boundary outcome: the `none` fallthrough and the `unapplicable`/`exec_failed` events are asserted, and `_run_locked` is driven with a REAL orchestrator to prove a declined boundary logs `self_upgrade_deferred`, leaves the record `pending` and keeps stepping — after which a continuous boundary execs that same record. The decline is pinned per instance and per sha, `--max-steps` is proved not to be handed out twice, and `_cmd_start` is shown refusing on the live lock an `execv` preserves, which is why the successor argv is `run --continuous` and not the verb that was typed. |
| 2026-08-31 | upgrade-01 | `test_self_upgrade.py`: every non-exec outcome is now driven twice — once for the refusal, once proving a LATER process execs the SAME record (parametrised over preflight_failed / unapplicable / exec_failed). The no-spin bound is asserted on the real orchestrator rather than on a settled status. Two fail-open cases are new: a restore write that fails REMOVES the record instead of leaving `execed` (so no false `self_upgrade_confirmed`), and a failed one-shot marker write leaves the pending record untouched rather than clearing it. A record with no `base_sha` is offered no boundary at all. |
| 2026-08-31 | upgrade-01 | `test_self_upgrade.py` pins the field every bound is keyed on: `base_sha` read back from JSON as `null`, an `int`, a `dict`, a `list` or `""`. Parametrised over all five — the orchestrator offers no boundary, `decline_self_upgrade` refuses rather than raising on its `in` test, `_self_upgrade_at_boundary` answers `self_upgrade_none` naming the type and writes nothing, and `_run_continuous` reaches iteration two, proving `set.add` on an unhashable sha can no longer end continuous mode one statement after a boundary carried on. `_defer_self_upgrade` is bounded the same way. |
| 2026-08-31 | upgrade-01 | `test_self_upgrade.py` pins the A/B race between the boundary's two reads of `pending_upgrade.json`: record B is saved over A between the loop's read and the decision's, and `_run_continuous` is driven with a REAL orchestrator so the suppression is asserted on `_self_upgrade_due` — one boundary entry, one `preflight_failed` naming B, iteration two reached and not offered again. The stand-in stops at three rounds so a lost bound fails the suite instead of hanging it. Also: every refusal names the record it acted on, `none` names none, and an unusable identity reads `""` rather than raising on `set.add`. |
| 2026-08-31 | val-08 | `test_agent_self_validation.py` §11 and the last section of `test_validation_parallelism.py` grade the relocated pytest cache. The central case runs a REAL failing pytest in a REAL repository through `run_validation_commands` and compares `git status --porcelain -uall` byte for byte across it — on the TREE, not on the absence of a directory name — and also asserts the cache landed at the chosen path, so a `python3` without pytest fails the test rather than passing it vacuously. Also pinned: `--lf` selects 1 of 13 tests, two tasks and two rounds never share a cache, and a broken cache root cannot leave a rerun flag behind. |
| 2026-08-31 | brw-19b | `test_transport_vocabulary.py` allows ZERO browser imports in `autoloop/*.py` and makes none itself; its population guard (`len(scanned) > 20`) is load-bearing now, because a clean tree and an unread one give the same empty offender list. `test_rounds_and_restart.py`'s unattachable-browser cases call `_recover_unattachable_browser` DIRECTLY — the probe they stubbed is gone — and a new case pins that nothing reaches it. `test_transport_recovery.py` proves the transcript observation summary over a LOCAL dataclass, so the claim is the orchestrator's and not the browser's. |
| 2026-08-31 | val-08 | `test_agent_self_validation.py` §11c gains three refusals the `cwd`-only check let through: a cache root in a sibling of a declared `validation_cwd`, the same shape driven through `_advisory_for` (which is what pins that the executor really passes `repo_root` — it defaults to `cwd`, so a caller that stopped would silently get the weaker check back), and a temp root symlinked into the tree. Each creates the declared cwd first, since a missing one answers NOT_RUN before the cache is reached and would grade nothing, and each asserts the run still launched with `-p no:cacheprovider`. |
| 2026-08-31 | val-08 | `test_validation_parallelism.py`'s deference case is INVERTED: all four spellings of a configured `cache_dir` are now replaced by the caller's, and two new cases pin the fail-closed cluster path and its idempotence. `test_agent_self_validation.py` §11a gains a second REAL failing pytest run, this one carrying `-o cache_dir=.pytest_cache` — the shape that reintroduced the 2026-08-03 defect — and asserts the same pair as its neighbour: the tree is byte-identical across the run AND the cache landed at the caller's path, so it cannot pass vacuously. |
| 2026-08-31 | val-08 | `test_agent_self_validation.py` §11f drives the three-run sequence review found: pytest fails and records, `ruff` then fails before pytest, and run 3 must be FULL. Asserted on the LAUNCHER — no rerun flag AND no list on disk when run 3's pytest started, since `--lf` over an empty list runs everything and reads identically in the answer text. Three more: a rerun never chains into another, an unclearable list (a non-empty directory in its place rather than a patched `unlink`) gives the whole cache up, and a request that executed nothing leaves the previous run's evidence alone. |
| 2026-08-31 | merge-09 | new `autoloop/tests/test_pre_extraction_candidates.py`, over real repositories from `gitrepo.make_repo_from_template`: a mapped sha that IS an ancestor resolves, one that is NOT stays unresolved, a 40-zero row is nothing-to-merge rather than merged, an absent row resolves nothing, the archived `<id>-reconciled-as-<sha>` filename answers an empty re-dispatched stub, and a `Merge task <id> (<sha>)` merge subject resolves a discarded candidate. |
| 2026-08-31 | merge-09 | the same file pins the fail-closed half, which is the only half that can fail silently: no map, an empty map, an ambiguous 7-hex prefix, an unorderable archive, a sibling id's label, a walk past its cap, a task id in a commit BODY, a one-parent commit with a merge-shaped subject, `brw-180` not answering for `brw-18`, and a candidate git CAN resolve still going to the remote and to the merger. |
| 2026-08-31 | merge-10 | new cases in `test_pre_extraction_candidates.py` for the archived-filename SCAN: the `audit-0001` shape (a newer `-report-recovered-by-operator-<stamp>` naming no sha over an older `-reconciled-as-<sha>-<stamp>`) resolves as PRUNED and the transcript names the copy that answered; the first MATCHING label answers even when an older one would have resolved; a dash-delimited hex-shaped run in a label the pattern does not cover clears nothing though the map holds a row keyed by it; and an unstamped copy still refuses the whole archive rather than being passed over. |
| 2026-08-31 | merge-10 | a fifth case there: a TIE in which only one copy names a sha is refused, not passed over — inside one generation there is no order to scan. And four pure-function cases in `test_merge_sweep.py` over `_archived_generations`, the ordering helper the scan and `execution_record_ancestry` now share: three stamps come back newest-first, a shared stamp is ONE generation holding both, one unstamped copy refuses every generation and names itself, and a single copy is one generation whatever its label reads. No repository — the rules read the filename and nothing else. |
| 2026-08-31 | brw-19c | `test_conversation_retirement.py` imports `autoloop.browser` nowhere. The client-level tests that ran the real `BrowserChatGPT.await_response` over a scripted page are removed with their fakes rather than restubbed — a stand-in raising the right error by fiat would prove only the stand-in. The orchestrator half is kept (park, no Chrome restart, no `consecutive_failures` charge; an escaping `SessionLostError` still restarts), including the `_browser_backed_provider` import without which every negative assertion passes vacuously, plus a new self-check on this file's own source and module namespace. |
| 2026-08-31 | brw-19c | `test_doctor.py` pins the removed browser checks by their ABSENCE — one `REMOVED_CHECKS` set asserted against the whole result list — and adds four boundaries: the retired provider name buys no special handling, an unbuildable seat FAILS instead of reporting `skip`, a `[browser]` section holding values the old checks called malformed still loads and is never graded, and `DoctorProbes` refuses a `probe_cdp` kwarg. `test_browser_provider_removed.py` gains a §2b for `restart_command`; `test_v1_smoke.py`'s doctor case needs no stubs now. |
| 2026-08-31 | brw-19c | Two vacuous-pass reads closed. `test_doctor.py`'s leftover-`[browser]` case made three assertions over `results` that all hold for an empty list; it now proves the sweep ran (`config`, `primary_live`) before asserting what it did not produce. `test_browser_provider_removed.py` looped over `RESTART_COMMAND_EXAMPLE` to check the pasteable line, and an emptied tuple checks nothing while `restart_command = []` still passes the rest, so it is asserted non-empty first. Its malformed-`restart_command` case is now parametrized over a non-list and two lists with non-string elements, so the elementwise half of the shape check is exercised too. |
| 2026-08-31 | brw-19c | `test_transport_vocabulary.py` gains `test_no_operator_advice_in_the_orchestrator_names_the_retired_package`: an import scan cannot see a module path handed to an operator as a COMMAND, which breaks nothing until someone follows it. Source-scanned over `orchestrator.py` alone — `conversation.py:385` holds the same path in a comment and is out of scope, so a package-wide scan would go red on a line this round may not edit — after asserting the file really was read, and every surviving `chrome_restart` must sit within 240 characters of "obsolete". |
| 2026-08-31 | brw-19a | `test_transport_vocabulary.py`'s import scan now reads every `.py` under `autoloop/` except `autoloop/tests/`, closing the `autoloop/codex/` carve-out that let the last two live imports sit green under a file whose whole job was to prove there were none; the regex already matched them, so it would have gone red on both the day it was widened. Population floors are asserted per SUBTREE, not only in total, because a rename that empties `codex/` reproduces the old carve-out silently. New `test_the_retired_browser_package_ships_no_module_at_all` checks source files AND `import_module`, per submodule. |
| 2026-08-31 | brw-19a | That test asserts about `autoloop/browser/*.py`, never `browser_dir.exists()`: an executor that unlinks files leaves the directory behind, and git tracks no directory, so an existence check grades a worker tree's leftovers and goes red on a correct change. The emptied directory then resolves as a PEP 420 namespace package, which imports and can hold no code — accepted explicitly, while a REGULAR package (an `__init__.py`, therefore code) is not. `sys.modules` is checked before each import so a cached module cannot satisfy the raises-check. |
| 2026-08-31 | brw-19d | `test_blockers.py` §7b is re-pointed off the retired browser rows onto `primary_live`/`provider`/`codex_command` and gains four states: seat unavailable, seat live, provider unregistered while `primary_live` is OK, and a sweep emitting neither required row. A raising sweep and an ABSENT optional row are pinned too — absent must pass, present-and-failing must refuse, which is the only thing separating "optional" from "ignored". |
| 2026-08-31 | brw-19d | `test_blockers.py` also pins the fail-only tier in both directions — a `fallback_live` of `warn` (the no-fallback default) resolves and one of `fail` refuses, with `primary_live`/`provider` OK on both arms so the fallback row alone decides. `test_doctor.py` asserts that default really is `warn` against a live sweep, because a doctor change to `fail` there would jam every single-seat deployment's transport blockers shut. |
| 2026-08-31 | brw-19d | New `test_blockers.py` §7c is the discriminating pair for transport independence: one dead-seat sweep, `login_expired` refused and `git_failure_budget_exhausted` resolved, with the doctor stub asserted UNCALLED for the second, plus a non-repo cwd proving the git code still refuses on its own condition. `test_doctor.py` derives the required and optional row coverage from `cli`'s tuples against a REAL `run_doctor`, so removing a row from doctor fails there instead of silently disarming the recheck. |
| 2026-08-31 | notify-01 | New `test_notify.py`: no SMTP, no network, no repository — the transport is INJECTED, which is the only way the two cases that matter are reachable at all. A transport that RAISES and one that HANGS are both driven through the real `heartbeat.publish`: the hang is cut off at `timeout_seconds` (asserted on elapsed wall clock) and the beat is still written. `smtplib`'s own socket timeout could not demonstrate either. Also pinned: each element of the change tuple notifies, an unchanged tuple does not, a restart re-sends nothing, and `enabled = false` reads and writes nothing at all. |
| 2026-08-31 | notify-01 | `test_notify.py` covers the fail-opens rather than the happy path: a dead server is NOT retried on the next beat (the tuple is recorded first), an unwritable state file still sends once (the in-process cache), a corrupt record SENDS rather than going quiet, park -> running -> park mails twice because the record is the last OBSERVED tuple and not the last mailed one, and every "no password" shape — unset, empty, missing file, blank file, no source, no username, both sources — refuses instead of sending unauthenticated. One case proves no password reaches a subject, body, log line, `repr` or the persisted record. |
| 2026-09-01 | policy-01 | New `test_policy_denial_budget.py` drives four consecutive bare `push` approvals through `run()` and the real phase machine, never `_handle_policy_denial` directly — the call shape that passed all through the 71-denial livelock. `_step_executing` cleared `policy_denials` as soon as `authorize_directive` ALLOWED a directive, one statement above a `_dispatch` that then refused it, so a denial raised INSIDE a dispatch always counted as the first. Pinned: the PERSISTED counter climbs 1-4, an acted-on directive still clears the streak, gate and dispatch denials are consecutive, and the budget still binds with autonomy on. |
| 2026-09-01 | postcommit-01 | New `test_postcommit_unbound_push.py` drives the 2026-08-31 reviewer through `_step_ready`/`_step_executing` on real git: two identical bare `push` replies, the first re-presenting the candidate and the second publishing it — one denial, not four. Also pinned: the packet REPLACES the correction and binds, charges no review round, the published state draws a refusal naming no approval shape, and a stale `state.task_execution` mirror re-presents nothing. Not widened: an unrenderable packet and an unnamed worker repo fall back to the plain refusal (never a park), four of those still exhaust the budget, `commit_and_push` untouched. |
| 2026-09-01 | halt-04 | New `test_refusal_revise.py` pins the resend and the bound separately, because they fail differently. Per code: the refusal goes back as a `revise` carrying its text VERBATIM, and the same refusal repeating sets the task aside. The bound is proved twice over — ten consecutive occurrences of one refusal produce exactly one revise, and a repeat arriving after the record was CLOSED is still recognised, by the digest `refusal_revises` counts on every record on disk. |
| 2026-09-01 | halt-04 | The regression `test_refusal_revise.py` exists for: an autonomous retry closes its blocker on the next COMPLETED step, and a revise round IS one — so closing it there would refund the meter every round and resend forever. Driven through `run`'s own `_close_recovered_blocker`. Fail-closed edges pinned: flag off, `max_recovery_attempts = 0`, no blocker store, a park naming no task (the changeset arms) or a bystander, an unknown task, a task with no approved plan, an empty refusal, and the five hard halts each park exactly as today. |
| 2026-09-01 | halt-04 | The same file covers the identity meter's own edges: a DIFFERENT refusal under the same code gets its own revise, an A-B-A sequence proves that letting it through refunds nothing, and the same refusal one PHASE along is still one allowance. Also pinned: an archived record does not refund, a revise the loop DECLINED to issue spends nothing, an unwritable meter parks (`meter_write_failed:OSError`), a non-list `revised_refusals` raises rather than reading as unspent, an empty identity can never be stored, and the ceilings that bound a churning refusal are themselves set-asides. |
| 2026-09-01 | intake-01 | New `test_audit_intake.py` — no git, no model, no subprocess, because every claim is about parsing, a JSON file and a directory. Pins the three outcomes: ALREADY DONE records evidence RE-READ off the tree and files no task, a finding an existing task names is recorded under that task with nothing queued, a decline survives an independent second read and leaves the outstanding list, and a promoted finding produces >1 approved path plus >=1 validation command aimed at the right suite. The dashboard panel is asserted to show OUTSTANDING findings only, through `app_tasks`/`audit_findings_panel` with an explicit `intake_dir`. |
| 2026-09-01 | intake-01 | The fail-opens in the same file: a missing file never satisfies a `path:!text` check (a deleted file makes any needle absent), an empty `--evidence` list is refused, a path resolving outside the checkout is refused, an unreadable ledger filters NOTHING and refuses the write rather than overwriting every decision in it, an `outcome` word the code does not know leaves its finding outstanding, a re-worded finding reopens, promotion is refused outright when `tasks.json` could not be read, and a spec that would carry one path or no validation raises instead of filing. One CLI case drives all three verbs through `cli.main`. |
| 2026-09-01 | intake-01 | Two more duplication cases, both refusing BEFORE the ledger is written: promoting a finding already recorded as promoted queues nothing a second time (an operator can still type an id `list` no longer offers), and a spec whose id the registry already holds is refused here rather than on merge, where the record would already claim a task exists. Also pinned: a report that is a directory and one that is not UTF-8 are notes rather than exceptions — `UnicodeDecodeError` is a `ValueError`, and both readers run on the dashboard's 2s poll where an escape is a 500. |
| 2026-09-01 | merge-05 | New section in `test_merge_window.py` for a record ORPHANED of its task: the measured `audit-0002` shape (no registry task, no worker directory, never published, a base `git filter-repo` made unplaceable) is EXCLUDED and reported with its reason and remedy, asserted on the note text and end to end through `merge-window`. The refusals are the point: in-progress and queued tasks, a worker at either the recorded or the conventional path, a recorded `published_sha`, an absent or empty `tasks.json`, an absent or unconfigured `workers_root`, and a task id that is not a legal path component or not a string. |
| 2026-09-01 | merge-05 | `test_merge_sweep.py` gains the same case end to end over real git: one orphaned record beside a published branch sweeps and logs the exclusion as `merge_sweep_window_note`, and the same record with a worker repo at `workers_root/<id>` still defers the WHOLE sweep. |
| 2026-09-01 | intake-01 | Same file, the queue the registry cannot see: two findings whose derived task ids collide (`db-01` and `db_01` both give `audit-db-migrations-db-01`, asserted so the fixture cannot stop colliding silently) are promoted against ONE undrained inbox, and the second is refused before either write — the first queued request is byte-identical afterwards and still names finding one, exactly one ledger record exists, and the second finding is still outstanding. Also pinned: `--task-id` really files it, the same id typed twice is caught, and a queued mutation or unparseable file claims nothing. |
| 2026-09-01 | select-04 | `test_test_selection.py` gains a block on opacity ATTRIBUTION. Each file that really spawns is asserted still opaque BY NAME, each that only mentions an interpreter is asserted not, and the whole set is an equality so a file JOINING it is caught too. Every per-file case also asserts the file was opaque BEFORE, so none of them can pass for one that never carried the signal. The pre-select-04 rule is recomputed in the test module, which is what lets the floor and the docs-only round be stated as before/after NUMBERS rather than as "fewer"; `test_prose_doc_selection.py` asserts the docs-only figure again where that round is the subject. |
| 2026-09-01 | select-04 | Same file: the fail-closed cases are CONSTRUCTED, never borrowed from a real file that could quietly stop spawning — a non-constant module name, `spec_from_file_location` with a constant where a name would go, an argv holding `*args`, an aliased `subprocess`, `from subprocess import Popen`, a spawn referenced without being called, and an `orch.run(max_steps=1)` sharing only a name. Plus a real-repository guard that fails the round introducing a file which reaches an interpreter through `os.execv`/`os.system`, a route the literal-only rule covered by accident. |
| 2026-09-01 | wanted-01 | new `autoloop/tests/test_wanted_decision.py` pins the claim with cheap tests where the subject is a pure function: the parser still accepts an omitted field on every decision, retired included, and `PROTOCOL_VERSION` is still 3; a blank answer parses as ABSENCE rather than raising, while a non-string is still `bad_type`; every active decision without an answer is denied `wanted_decision_missing` and the same directive WITH one never is; a retired decision keeps its own denial; and no policy flag can admit an unanswered directive. |
| 2026-09-01 | wanted-01 | Same file, real repository only where the claim is about disk: one ordinary round now leaves `wanted_decisions.json` behind — the measured failure, since it had never been created — a `stop` naming `recut` as the wanted verb is COUNTED with the candidate untouched, an unanswered reply is re-prompted and executes nothing and writes no tally row, and eight unanswered replies end the run on the DENIAL budget with not one `parse_error` logged. Every other suite's `block()` helper now defaults the field; `test_recut.py`'s deliberately does not. |
| 2026-09-01 | prov-01 | new `autoloop/tests/test_codex_stdout_verdict.py` builds the DECORATED stdout captured on 2026-08-17 — hook lines, role markers, trailing token count, duplicated verdict — and pins it against a verbatim copy, because a clean-output fixture proves nothing about this CLI. It asserts the old path still fails (`invalid_json`), that the same decoration around a COMPLETE push directive parses to one `push` with its stamp intact, and that a complete but DIFFERENT directive echoed under the `user` marker is never the verb that comes back. No codex binary is involved. |
| 2026-09-01 | prov-01 | Same file, the failure paths: a `codex` marker with no message and two `codex` messages each fail the invocation with a named note and stash nothing for `await_response`; a prose reply is isolated and then refused BY THE CONTRACT, so isolation is never mistaken for validation; a hook line quoted inside a JSON value, a blank line inside a message and a message that runs to end-of-stdout are all handled; undecorated stdout is passed through byte for byte and claims no isolation, while stdout that is ONLY hook and token lines is refused rather than passed on to be refused more expensively by the parser. |
| 2026-09-01 | prov-01 | `SUITE_SIZE` moves 103 -> 104 in `test_test_selection.py` and `test_prose_doc_selection.py`, the DENOMINATOR only and for wanted-01's reason: the new file reads no tracker and spawns no interpreter, so a docs-only round still selects the same 20 of the suite and the floor is unmoved at 16. Both constants carry the arithmetic in a comment beside the previous bump rather than being silently re-snapshotted. |
| 2026-09-01 | prov-01 | Same file, the echo bounds, one test each: a transcript quoted INDENTED in the prompt — the shape this task's own description carries — still yields one `push`; a bare `codex` line in the prompt's prose does too; the same transcript FLUSH LEFT is two messages and refused without the prompt, one `push` with it; two genuine reviewer messages stay refused when the echo is anchored; a `user` line mid-message still leaves a boundary, so two messages cannot collapse to one; a transcript whose only marker is echoed is REJECTED, where reading it would have given `commit_and_push`. |
| 2026-09-01 | prov-01 | Same file, the containment bound at both seams: a reflowed echo quoting a flush-left directive stamped for THIS round, with an empty reviewer message, leaves `isolate_reply` empty at `echo_anchor: unmatched`, `segments: 1`, and `submit` REJECTING with nothing stashed. Two variants, one per arm — the directive byte-identical, and hard-wrapped over two lines (asserted absent from the prompt literally, so only the squeezed arm can catch it). Each counterfactual parses to a `commit_and_push` carrying this round's request id. A candidate that squeezes to nothing pins the literal arm, the one case it carries alone. |
| 2026-09-01 | prov-02 | New `autoloop/tests/test_codex_preflight.py` fakes the invocation boundary rather than the binary, which is the only level at which "an exhausted allowance is classified as an allowance" can be asserted without actually being out of quota. It pins: an unresolvable command fails and spawns nothing; a refused working directory fails naming codex's own words and BOTH remedies; a spent window and a 429 are `warn` and name themselves; empty configured pattern lists still classify (the `or DEFAULT` fallback). |
| 2026-09-01 | prov-02 | Same file, the fail-open half: a probe that raises (timeout, permission, missing file) and a probe returning something unreadable are both `fail` naming the fault, never a pass; stdout that is only the preflight's own prompt coming back is `warn` "no message could be isolated", never a reply; and the preflight prompt is asserted clean against BOTH built-in pattern lists, because a prompt mentioning a limit would be suppressed by the very echo guard that makes the classification trustworthy. |
| 2026-09-01 | prov-02 | `test_doctor.py`: `probes()` now stubs the preflight by DEFAULT — `make_config` configures a codex seat, so an unstubbed bundle would launch a real `codex exec` in every test in the file — and the same stub was added to `test_v1_smoke.py`'s doctor sweep. New rows pinned: a spent allowance is `warn` and leaves `exit_code` 0, a working dir inside the repository is refused WITHOUT asking the probe, a missing configured dir is never created, and doctor's graded path equals the runner's cwd. |
| 2026-09-01 | prov-02 | `SUITE_SIZE` moves 104 -> 105 in `test_test_selection.py` and `test_prose_doc_selection.py`, the DENOMINATOR only and for prov-01's reason: the new file reads no tracker and spawns no interpreter, so a docs-only round still selects the same 20 and the floor is unmoved. Both constants carry the arithmetic beside the previous bump rather than being silently re-snapshotted. |
| 2026-09-01 | prov-02 | `test_codex_provider.py` pins the sandbox policy against the argv `subprocess.run` REALLY receives, through the production factory from an `AutoloopConfig` — `argv_preview` is a diagnostic string and proves nothing about a process — and pins that an empty or bypass `sandbox_args` spawns nothing at all. `test_codex_preflight.py` adds the fail-closed table (four `--sandbox` spellings, `--full-auto`, two bypass flags, a near-miss mode, a dangling option, a non-string token, weakest-wins) and that an unconfined policy fails the preflight without invoking. |
| 2026-09-01 | prov-02 | `test_doctor.py`: `codex_sandbox` is asserted to grade the POLICY rather than whether the list is non-empty — read-only `ok` (and the row itself must say read-only does NOT confine reads), workspace-write `warn`, empty/bypass/unknown `fail` — and an unconfined seat leaves `codex_preflight` `fail: not attempted` with the probe never asked. No new test file, so `SUITE_SIZE` stays 105 and the selection floor is unmoved. |
| 2026-09-01 | prov-02 | `test_codex_preflight.py` gains the pin for that correction: the read-only row must SAY writes are restricted, reads are not confined and commands still run, and must not carry any of five retired overclaim shapes — checked over all four `--sandbox read-only` spellings, because the sentence lives in one branch and an assertion naming one spelling would still pass a fix applied only there. A second test sweeps every grade plus the preflight refusal. `test_doctor.py` asserts the same three on the row an operator reads. No new file, so `SUITE_SIZE` stays 105. |
| 2026-09-01 | prov-02 | Working-directory provenance is pinned as a PAIR over ONE absolute path in `test_codex_preflight.py`, `test_codex_app_server.py` and `test_doctor.py`: spelled out it is refused, unset it is created. Every refusal arm asserts the directory still does not exist — a bare `raises` also passes for code that creates it and then raises. The runner arm drives `SubprocessCodexRunner.run`, since the claim is that provenance survives construction-then-spawn, and `"  "` is pinned as NOT unset, so a stripping predicate cannot create a directory in the loop's own cwd. No new file, so `SUITE_SIZE` stays 105. |
| 2026-09-01 | ctx-02 | New `autoloop/tests/test_context_records.py`: every named refusal of the record contract, and the delegation to `tasks._validate_approved_path` asserted as a CALL through a spy — two implementations agreeing on today's sample is what drift looks like before it happens. Real git for the stamping claims: one run writes HEAD, a second changes nothing, and a missing pointer, a malformed record or a HEAD that is not a full sha each leave every file untouched. The seeded records are graded against a HEAD the test resolves ITSELF, and there is no `skip` in the file: skipping without git would retire the check that proves no sha was typed by hand. |
| 2026-09-01 | ctx-02 | `SUITE_SIZE` moves 105 -> 106 in `test_test_selection.py` and `test_prose_doc_selection.py`, the DENOMINATOR only: the new file builds real repositories but spawns no interpreter, and deliberately spells no tracker path in evaluated code, so the docs-only 20 and the floor of 16 are unchanged. Only `docs/context/project.md` and the category `README.md` basenames are named in code, so a change to any other seeded record is attributed to nothing and widens the run — the fail-closed direction, and the same treatment any new document gets. |
| 2026-09-01 | ctx-02 | `test_context_records.py` gains the resolution claims: a commit this checkout holds loads, `b`*40 is refused as `unknown_commit` naming the record and the sha (asserted to pass the regex first, so the git probe is what caught it), a blob's oid from `hash-object` is refused because `object_exists` answers True for ANY object, and a gateway whose every probe raises is a refusal, never a pass. A spy pins that the loader ASKS; an exploding gateway pins that an all-UNSTAMPED tree asks nothing. The already-stamped test now holds a real commit while HEAD moves past it. No new file, so `SUITE_SIZE` stays 106. |
| 2026-09-01 | ctx-02 | `test_context_records.py` gains the three shapes where the worktree says yes and the commit says no — an untracked file, one staged and never committed, one deleted from HEAD and restored on disk — each a real repository whose worktree and HEAD disagree, each asserting the refusal AND that every record still reads UNSTAMPED. The inverse is pinned too: a path the commit holds stamps even after the worktree loses it. Plus the prefix boundary (`pkg/mo` is not `pkg/mod.py`), a directory pointer, an unlistable tree, a nothing-pending run that lists none, and a mid-run change leaving both files untouched. Still no new file. |
| 2026-09-01 | ctx-02 | Measured on the way: a constrained glob LITERAL in evaluated test code makes that file a declared READER of every document it matches (`validation._names_document`), so an `rglob("*.md")` inside a new assertion helper moved a docs-only round from 20 selected files to 21 and failed both selection tests. A bare `rglob("*")` discriminates nothing, is dropped by `_glob_constrains`, and is the right sweep whenever the filtering is done in Python anyway. |
| 2026-09-01 | ctx-02 | `test_context_records.py` adds the one category the contract cannot reach: a record hidden as `.hidden.md` under `docs/context/` is stepped over by the loader, and `check` must NAME it on stderr while still exiting 0. A report, not a refusal — `.DS_Store` is not a broken record — but silence would leave a file in the tree that nothing validated and nothing mentioned. No new file, so `SUITE_SIZE` stays 106. |
| 2026-09-01 | ctx-02 | Correction to the line above, which pinned a bypass as if it were the contract: that exemption was filename-first, so ANY record left the one validator by gaining a leading dot and `check` still exited 0 over a tree holding it. The exemption is now non-Markdown droppings only, and the test asserting exit 0 uses `.DS_Store`. |
| 2026-09-01 | ctx-02 | `test_context_records.py` pins that regression eight ways: a malformed hidden record refused by name, an unindexed hidden record refused, a valid indexed `.hidden.md` loading AS a record (so "refused" is not reached by refusing every dotted name), a file named `.md` parsed rather than read as a dotfile (`Path(".md").suffix` is `''`), `.README.md` not borrowing the navigation exemption, `.hidden.MD` and `NOTES.MD` held by the case-folded match, and `check` exiting 1 and naming the path rather than printing "ignored". No new file, so `SUITE_SIZE` stays 106. |
| 2026-09-01 | ctx-02 | Same file, the second silent skip — found by hunting the first: the sweep kept only regular files, so a DANGLING symlink under `docs/context/` was stepped over without a word, being False to both `is_file` and `is_dir`. Two tests pin the refusal: `features/ghost.md` pointing nowhere, and a link that RESOLVES, refused too because a record validated as whatever it points at today is a different record tomorrow. Still no new file, so `SUITE_SIZE` stays 106. |
