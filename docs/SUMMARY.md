# SUMMARY.md

File-level index of this repository. "Where do I look to change X?" is answered
here. Update it whenever a file is added, removed, or changes responsibility.

| Path | Purpose |
|---|---|
| `autoloop/orchestrator.py` | The loop's spine: phases, dispatch, review, merge, self-upgrade. |
| `autoloop/cli.py` | Every subcommand, and the operator surface. |
| `autoloop/tasks.py` | Task registry, dependency graph, approved paths. |
| `autoloop/validation.py` | Validation commands and per-commit test selection. |
| `autoloop/packet.py` | Review-packet construction. |
| `autoloop/context_packet.py` | The per-round context packet: rendered at the worker's base sha, hashed, stored beside `executions/`. |
| `autoloop/git_gateway.py` | Every git call, behind a policy. |
| `autoloop/note_merge.py` | Append-only change-note conflict resolution. |
| `autoloop/per_test_deps.py` | Which modules ONE test can reach, statically. |
| `autoloop/pytest_deselect.py` | pytest plugin: drops the tests a narrowed round does not need. |
| `autoloop/codex/` | The Codex transports (subprocess and app-server). |
| `autoloop/audit/` | Read-only audit agents and task generation. |

## Change notes — append ONE new line at the END of this file

<!-- CHANGE-NOTES: append below, one line per note, at the END of the file. -->

| Date | Task | Note |
|---|---|---|
| 2026-08-27 | split | extracted from language-app with git filter-repo, 414 commits of history preserved; this tracker seeded here. |
| 2026-08-27 | select-02 | `autoloop/validation.py`: a changed `.md` is now attributed by `_files_reading_documents` — the files that READ it (exact name in evaluated code, plus `__file__`) — instead of `_reference_tokens`, whose bare `docs` and `.md` tokens made the change note every task must write select the whole suite. `.toml` and every other unresolvable path keep the token rule; the closure, the union and every widening rule are unchanged, and a document nothing reads still widens. |
| 2026-08-27 | select-03 | `autoloop/validation.py`: `_names_document`'s glob branch now requires the pattern to keep a literal character. A glob of only wildcards matches every path, so it names no document. `dashboard.py:4026`'s `audit_dir.glob("*")` — a listing of audit RUN directories — matched all six trackers, made that module a declared reader of each, and the closure over it then selected 72 of 93 test files on any prose change. Measured: docs-only 72 -> 20, `validation.py` plus both trackers 80 unchanged, `docs/audit_charters.toml` 93/93 unchanged. `*.md` and `docs/AUDIT_*.md` are unaffected. |
| 2026-08-28 | perf-01 | `autoloop/validation.py`: `_file_is_opaque` and `_imported_modules` each walked every parsed file separately, and `ast.walk` was 63% of a `select_validation_commands` call. They are now one `_scan_module` returning `(opaque, imported)` from a single walk: 0.98s -> 0.63s over this checkout's 159 files. WHICH nodes count did not change, and the two functions' agreement with the merged one was checked file-by-file across the whole checkout before the split was removed. Prose naming either function was updated to name `_scan_module`. |
| 2026-08-28 | perf-01 | `autoloop/validation.py`: two more tree walks out of the selection path. `_code_strings` collected docstring ids and string constants in separate `ast.walk` passes and now does one, applying the prose filter after it. `_addresses_own_checkout` is not called at all when the source lacks the literal token `__file__`: an identifier cannot be spelled any other way, so absence of the text is a necessary condition and the skipped walk could not have found a `Name` — 27 of 159 files still need it. Both checked against the previous implementations over every file in the checkout. Six real-repository selections: 19.69s -> 15.09s. |
| 2026-08-28 | doc-drift | `autoloop/orchestrator.py`, `autoloop/config.py`, `autoloop/config.example.toml`: three copies of "only the POST-COMMIT re-run narrows" outlived val-04, which made the pre-commit run narrow too. `_run_post_commit_validation` claimed to be the only narrowing call site and priced a round at one full suite plus one narrowed; the template said narrowing pre-commit needed changes that had landed (`cli.py` threads `test_selection` to the executor); `AuditConfig.test_selection` documented one phase. All three now name both. `PRECOMMIT_EVIDENCE` was already right and is untouched. |
| 2026-08-28 | perf-01 | `environment.snapshot()` reads every config key from ONE `git config --list -z` (new `GitGateway.config_map`) and resolves the hooks directory once for both hook sets, instead of eight subprocesses reading eight consecutive instants — a correctness point before a cost one, since a snapshot should describe ONE instant and `verify_unchanged` compares two of them. `push_exact`'s repeated reads are deliberately NOT batched: they are separated in time for TOCTOU. `-z` because a config value may contain a newline; `--list`/`-z` added to the policy allowlist. `--list` LOWERCASES keys (`core.hookspath`), pinned by a new test. Suite git spawns 45,289 -> 41,942. |
| 2026-08-28 | perf-01 | New `autoloop/per_test_deps.py` decides STATICALLY which modules one test can reach — its own name references closed over the helpers, classes, autouse fixtures and sibling-test-module imports of its file. Nothing is wired to it yet. Measured prize if it were: a change to `dashboard.py` runs 1,824 tests instead of 2,896 (-37%), `note_merge.py` -30%, `validation.py` -22%; core modules like `git_gateway.py` only -5%, because nearly every test genuinely reaches them. Everything unresolvable is OPAQUE and always selected, the same fail-open shape `_scan_module` uses for files. |
| 2026-08-28 | perf-01 | New selection mode `[audit] test_selection = "per_test"`, DEFAULT OFF. It keeps `reachable`'s file set EXACTLY and additionally deselects the tests within those files that `per_test_deps` says reach nothing changed, via `autoloop/pytest_deselect.py` and a drop-list named on the command. A drop-list, not a keep-list: anything the analysis fails to name then RUNS. Nothing is dropped from a test file the commit changed, from a file selected by attribution rather than reachability, or when the list cannot be written. Measured: `dashboard.py` drops 1,072 tests, same 72 files. |
| 2026-08-28 | perf-01 | `per_test_deps` now fails open on two shapes it does not model: `parametrize(..., indirect=True)`, where the parameter names a FIXTURE resolved at run time, and a `conftest.py` above the file declaring an autouse fixture, whose body lives elsewhere and runs for every test beneath it unnamed. This checkout's conftest declares none today — which is exactly why the soundness gate could not have caught it, so it is closed by reading rather than by observation. |
| 2026-08-28 | perf-01 | Audit units are minted `autoaudit-<iteration>`, not `audit-<iteration>` (`contract.AUDIT_UNIT_PREFIX`, with `is_audit_unit` replacing four hand-rolled `startswith` checks in `orchestrator.py`). A roadmap task may legitimately be called `audit-0003`, and the iteration counter RESETS each session, so a restarted loop re-minted ids it had used before: the completed roadmap task `audit-0001` and a fresh unit of the same name showed as one id in the merge backlog and the dashboard. The old spelling is still recognised, never minted — shipped records are not rewritten. Not retroactive. |
| 2026-08-28 | perf-01 | `audit/executor.py::_run_validation` now normalises its argv through `effective_validation_command`, as `run_validation_commands` already did for every other path. It ran the CONFIGURED command raw, so a pytest command got no `-n auto` and no `-p no:cacheprovider`: observed, an audit sat on the full suite SERIALLY for over fifteen minutes where eight workers finish it in about seven. Nothing failed and nothing reported it — the audit validates the whole repository to prove its one markdown file broke nothing, so it paid this on every run. |
| 2026-08-28 | port-04 | New `python -m autoloop projects`: one status view over every loop in `[projects].configs`, built by `dashboard.projects_status` and documented in a new `docs/AUTOLOOP.md`. Input is a list of CONFIG PATHS, so a fifth project is a config line. Read-only, lock-free, opens no checkout, bytecode writes off. `loop_state` (live/paused/stopped/unknown) is a FACT about the lock, kept apart from `health`'s verdict, because `blocked` fires while continuous mode works other tasks. Blockers are counted directly — `_judge` returns `stale_lock` before it reads them. One unreadable project is a row, never the page. |
| 2026-08-30 | upgrade-01 | `autoloop/cli.py`, `autoloop/orchestrator.py`, `autoloop/auto_merge.py`: every self-upgrade boundary now ends in a `self_upgrade_<outcome>` entry, and no non-exec outcome ends the process. The single-round `_run_locked` answered `SELF_UPGRADE` by returning — no event, record left `pending`, session cut mid-round — which is how 2026-08-27's 08:13:47 boundary took the loop down; it now logs `self_upgrade_deferred` and steps the SAME orchestrator again, bounded by `Orchestrator.decline_self_upgrade`. `deferred` is deliberately not a record status, so the next `start` still upgrades. The successor's argv is `run --continuous`, rebuilt, never the verb typed. |
| 2026-08-31 | upgrade-01 | Follow-up: a refused handoff no longer settles the record. `unapplicable`, `preflight_failed` and `exec_failed` are OUTCOME names only (`cli._carry_on_upgrade`) — each logs its entry and leaves the record `pending`, so a later process performs the upgrade exactly as 08:15:30 did on 2026-08-27. The spin bound lives in the process instead: `decline_self_upgrade` plus `_run_continuous.answered_upgrades`, and `_self_upgrade_due` refuses a record with no `base_sha`, which nothing could key a decline on. A refusal after the `execed` marker restores `pending`, or REMOVES the record if that write fails — never leaves `execed` to be confirmed falsely. |
| 2026-08-31 | upgrade-01 | `auto_merge.upgrade_bound_sha` is now the ONE predicate over `PendingUpgrade.base_sha`, shared by `orchestrator._self_upgrade_due`, `cli._self_upgrade_at_boundary` and `cli._defer_self_upgrade`. Each makes its own read of a mutable `pending_upgrade.json`, and a non-string value there killed the process between `self_upgrade_boundary` and any outcome: `[:12]` raises for a dict, an int or null, `set.add` and `in` raise for a list. Such a record is refused at the boundary as `self_upgrade_none` naming the type, nothing is written, and the loop carries on with the code it has. |
| 2026-08-31 | upgrade-01 | `cli._self_upgrade_at_boundary` now returns `UpgradeOutcome` — the outcome slug (a `str` subclass, so every comparison is unchanged) carrying the `base_sha` and `candidate_sha` of the record it ACTED ON. `_run_continuous` read the record before the boundary and the decision reads it again; a record swapped in between the two reads was refused, left `pending` and never declined, so `_self_upgrade_due` offered it again every round. Both shas are declined now — the extra one costs a retry by the next process, declining neither spins. An unusable identity reads `""` and never reaches `set.add`. |
| 2026-08-31 | val-08 | `autoloop/validation.py`: pytest's cache is a LOCATION question, not an on/off one. `effective_validation_command` takes keyword-only `cache_dir` and `rerun_last_failed`; with the defaults it is byte-identical to before, so the verdict run, the post-commit re-run and `audit/executor.py` are untouched. Given a `cache_dir` it REMOVES `-p no:cacheprovider` — the shipped config spells that flag out, so deferring to it would have made the feature inert on every real deployment — and injects `-o cache_dir=<abs>`. `--lf` is injected only inside that branch, so it can never reach a run with the plugin off, where it is exit 4. |
| 2026-08-31 | brw-19b | `autoloop/orchestrator.py` no longer imports `autoloop/browser/`: `attachable_page_targets`, the `_attachable_page_targets` wrapper and both call sites are gone, so `_classify_rate_limit_state` tells apart two worlds (`throttled`/`cleared`) from the held page alone and dispatches to no recovery. `_recover_unattachable_browser` is RETAINED and dormant: it is the only emitter of the `browser_unattachable` park code, which `cli._RESOLUTION_PRECONDITIONS` keys a recheck on, and neither of those files was in scope. A restart reporting success is now believed, not re-probed. |
| 2026-08-31 | val-08 | `autoloop/implement_executor.py`: `AdvisoryValidation` mints a per-ROUND pytest cache with `tempfile.mkdtemp` outside every worker repo, so no two tasks — and no two rounds of one task — share a `lastfailed`. `--lf` is asked for on EVIDENCE, a failed run whose failures the cache really recorded, so a round whose ruff failed before pytest (or whose `pytest.ini` disables the plugin where an argv rewrite cannot see it) runs FULL rather than exiting 4. Every other state is FULL with `-p no:cacheprovider`. A `--lf` PASS is stamped in the answer, in `note()` and in the description: it is NARROWER than the verdict run. |
| 2026-08-31 | val-08 | `autoloop/implement_executor.py`: the cache's "outside the worker tree" check now sees the REPOSITORY, not only the directory the commands run in. `AdvisoryValidation` takes `repo_root` (`_advisory_for` passes `git.repo_root`) and `_ensure_cache` compares both trees, in both nesting directions, over written AND resolved paths. A task declaring a `validation_cwd` runs in a SUBDIRECTORY, so a `TMPDIR` in a sibling of it was outside `cwd`, accepted, and inside the tree the post-commit gate reads; a symlinked `TMPDIR` was lexically outside and physically inside. Both are refused now, falling back to `-p no:cacheprovider`. |
| 2026-08-31 | val-08 | `autoloop/validation.py`: an advisory `cache_dir` REPLACES a configured `-o cache_dir=` instead of deferring to it. Deferring injected nothing for such a command — not the caller's path, not `-p no:cacheprovider` — so a relative configured value (pytest resolves it against rootdir) put `.pytest_cache/` back in the worker tree, and an absolute one is shared by every task. The strips are structural, so anything still mentioning `cache_dir=` or `no:cacheprovider` (a cluster like `-qocache_dir=.x`) falls CLOSED to today's behaviour with no `--lf`; that check is a SUBSTRING test, since a refusal must not switch itself off on a spelling it cannot parse. |
| 2026-08-31 | val-08 | `autoloop/implement_executor.py`: `--lf` is authorised by the run that RECORDED the failures, not by the file being there. `AdvisoryValidation` clears `lastfailed` before every non-rerun run, and the next request is narrowed only if that run started with it absent, ended not green, and left a non-empty list. Reading the file alone let a run whose `ruff` failed before pytest — the run stops at the first failing command — hand an EARLIER run's list to the request after it, narrowing it to failures two edits old. A clear that cannot be proved gives the round's cache up. |
| 2026-08-30 | merge-08a | `docs/extraction/commit-map.tsv`: filter-repo's 624-row map, vendored because `.git/` is not cloned and no worker could reach it. |
| 2026-08-30 | merge-08a | `docs/extraction/commit-map.tsv`: filter-repo's 624-row map, vendored because `.git/` is not cloned and no worker could reach it. |
| 2026-08-30 | merge-08a | `scripts/autoloop_health_notify.sh` and the five `docs/AUDIT_2026-*.md` came over from language-app f65c7af; `--path autoloop/` had left them behind. |
| 2026-08-31 | merge-09 | `autoloop/merge_sweep.py`: a completed task whose `candidate_sha` is not an object in this repository is now judged through the vendored `docs/extraction/commit-map.tsv` — a mapped sha by ancestry, 40 zeros as PRUNED (nothing to merge, NOT merged), no row as still unresolved — plus the newest archived `-reconciled-as-<sha>`/`-merged-as-<sha>` filename when the live record names no candidate, and, only once the publication check has failed, a `Merge task <id> (<sha>)` merge subject on the base head's first-parent chain. Nothing here merges: every verdict is a skip or a name. |
| 2026-08-31 | merge-10 | `autoloop/merge_sweep.py`: the archived-filename rule now SCANS the archived copies newest-first for a label the pattern applies to instead of inspecting only the newest generation and giving up. A copy whose name carries no sha is a file the pattern was never asked of, not evidence that no label names the commit — which held `audit-0001`, whose newest copy is `-report-recovered-by-operator-<stamp>` over an older `-reconciled-as-07b659b-<stamp>`. Among labels that DO carry a sha the newest still wins; the pattern and the tie and ordering refusals are unchanged. |
| 2026-08-31 | brw-19c | `autoloop/doctor.py` grades no browser. The unconditional `cdp` and `playwright` probes, the three checks keyed on the retired `browser_chatgpt` name (`conversation_url`, `conversation_active`/`conversation_rotations`, `project_url`), `_read_state_quietly`, both URL regexes and the `probe_cdp`/`playwright_present` fields of `DoctorProbes` are gone, so `doctor` no longer exits 1 on every machine without a dedicated Chrome. `config.RESTART_COMMAND_REPLACEMENT` named `autoloop.browser.chrome_restart` and is replaced by the placeholder `RESTART_COMMAND_EXAMPLE`. NO `[browser]` key was removed. |
| 2026-08-31 | brw-19c | `autoloop/cli.py`: `_precondition_browser` filters on the new `_BROWSER_PRECONDITION_CHECKS`, led by `primary_live` (emitted unconditionally at `doctor.py:453`). With doctor's `cdp`/`playwright`/`conversation_url` results gone, a whitelist naming only those matches nothing, and an empty match reads as healthy — `login_expired`, `submission_ambiguous`, `git_failure_budget_exhausted` and `browser_unattachable` would have become resolvable by answer text alone. `cdp`/`playwright` stay in the list because out-of-scope `test_blockers.py` stubs the sweep and pins them. |
| 2026-08-31 | brw-19c | Kept deliberately, and each is why: `cli._repair_browser` and `cli._default_probe_cdp` run only an OPERATOR-declared command and name no module in this package; `smoke-browser` stays registered and inert so a typed-from-memory invocation gets a sentence rather than a usage error; `config.BrowserConfig` keeps every key and default, so an unmigrated `[browser]` section still loads and `restart_command` is still stored exactly as written. `config.example.toml` now says `doctor` reports nothing about a browser. |
| 2026-08-31 | brw-19c | LEFT STANDING as a hand-off, not an oversight: `orchestrator.py:3299`, `:11381` and `:11468` still tell an operator to run `python3 -m autoloop.browser.chrome_restart`. Prose, not an import, so nothing breaks until brw-19a deletes the package — but `autoloop/orchestrator.py` is not in brw-19c's approved paths, and the strings are pinned from `test_rounds_and_restart.py:1062,1069` (the second over `question[:160]`) and `test_transport_fault_recovery.py:908`, neither approved either. Rewording without all three files in one commit turns three green tests red. Recorded at `test_transport_vocabulary.py`. |
| 2026-08-31 | brw-19c | SUPERSEDES the line above, on review: `autoloop/orchestrator.py` IS in this task's approved paths, so all three sites were reworded in this commit. Neither park nor `_browser_restart_outcome`'s docstring names a module in the package now; both parks still say `chrome_restart`, as OBSOLETE, and send the operator to their own `browser.restart_command` or to reopening the profile window. Keeping the word is what holds `test_rounds_and_restart.py:1062,1069` (the second over `question[:160]`) and `test_transport_fault_recovery.py:908` green without editing files outside scope. |
| 2026-08-31 | brw-19a | `autoloop/browser/` is deleted — all seven modules (`__init__`, `chatgpt`, `chrome_restart`, `observation`, `playwright_session`, `selectors`, `session`) unlinked, and its row removed from the index above. The two live importers left standing by brw-19b/brw-19c, `autoloop/codex/conversation.py:83` and `autoloop/codex/app_server_conversation.py:62`, now read `from ..conversation import SubmitResult` — the enum has been defined there since brw-17, so object identity is unchanged and every `is` check in the orchestrator still holds. No `[browser]` config key, blocker or recovery path was touched. |
| 2026-08-31 | brw-19a | LEFT STANDING, deliberately: `autoloop/conversation.py:385` and `autoloop/config.py:364` still spell `python3 -m autoloop.browser.chrome_restart` inside COMMENTS — one quoting a park a `codex_cli` run wrongly wrote on 2026-08-22, one describing what replaced the retired `browser.restart_command` example. Prose, not imports, so nothing breaks; both files are outside brw-19a's approved paths. `codex/conversation.py:163`/`:345` still name the `browser_chatgpt` SEAT, retired by brw-16 and preserved on purpose. Recorded at `test_transport_vocabulary.py`. |
| 2026-08-31 | brw-19d | `autoloop/cli.py`: `_precondition_browser`/`_BROWSER_PRECONDITION_CHECKS` become `_precondition_transport_live` over a REQUIRED tuple (`primary_live`, `provider`) that must be PRESENT and `ok`, plus an optional `codex_command` graded only when emitted. A missing required row, a failing row, and a sweep that raises now all refuse — closing the fail-open where a whitelist naming rows doctor no longer emits matched nothing and read as healthy. `login_expired`, `submission_ambiguous` and `browser_unattachable` route here; no alias is left behind, so the M1 identity test still discriminates. |
| 2026-08-31 | brw-19d | `autoloop/cli.py`: a third grading tier, `_TRANSPORT_PRECONDITION_FAIL_ONLY_CHECKS`, holds `fallback_live`. The loop runs real rounds on the fallback seat after a quota failover, so a `login_expired` raised there would otherwise clear on the primary seat's health — a recheck aimed one seat over from the park. It cannot be REQUIRED `ok` either: doctor reports `warn` when no fallback is configured, so requiring it would jam every single-seat deployment shut forever. `fail` is the only status that is neither. |
| 2026-08-31 | brw-19d | `autoloop/cli.py`: `git_failure_budget_exhausted` leaves the transport recheck for the new `_precondition_git_health`. A live reviewer seat was neither necessary nor sufficient evidence about git — the old mapping made the blocker unanswerable while the transport was down and clearable while git was still broken. The new check reads branch and head through `GitGateway`/`PolicyEngine` at `Path.cwd()`: necessary, not sufficient, since worker-repo and remote failures are unreachable from a question-only precondition. The entry is KEPT, so the code is still never resolvable on answer text alone. |
| 2026-08-31 | brw-19e | `autoloop/codex/protocol_errors.py`: the paragraph explaining why a transient throttle is not raised as `RateLimitedError` described a path brw-19b deleted — a CDP dial, a page-target count, a `loop_fatal` park on `browser_unattachable`. It now names the two worlds `_classify_rate_limit_state` really tells apart, and keeps the "no codex transport raises `RateLimitedError` today" claim `orchestrator.py:3381` and `test_transport_fault_recovery.py:944` cite this module for. Runtime routing is untouched. |
| 2026-08-31 | brw-19e | The same module's `QuotaExhaustedError` message no longer says the browser fallback can still run: no registered seat draws on the ChatGPT conversation quota, so it says codex_cli and codex_app_server both spend THIS allowance and the default empty `conversation.fallback_provider` parks. "separate quota" survives, which is what `test_codex_app_server.py:406` pins. `docs/AUTOLOOP.md`'s per-project isolation list reads "separate reviewer transport" where it said "separate browser profile". `codex/conversation.py:344` carries the SAME sentence and is out of scope. |
| 2026-08-31 | brw-19e | SUPERSEDES the two lines above, on review: `codex/protocol_errors.py` documents only CURRENT behaviour now. The paragraph recounting brw-19b's deleted CDP dial and `browser_unattachable` park is gone, and two `BrowserError` mentions drop the "still spelled that way" gloss, matching the untouched line beside them. `QuotaExhaustedError` no longer says a fallback buys no quota; it states that codex_cli and codex_app_server spend the SAME allowance, so a handover between them waits on the same reset. "separate quota" stays in one literal, which `test_codex_app_server.py:406` pins. |
| 2026-08-31 | brw-19e | LEFT FOR A FOLLOW-UP TASK, both outside these approved paths: `orchestrator.py:3577` still advises setting `conversation.fallback_provider` "(the browser provider draws on a separate quota)", and `_handle_quota_exhausted` parks with `f"{exc} {extra}"`, so that clause is read as one paragraph with the message above it; `codex/conversation.py:345` makes the same claim in the codex_cli seat's own `QuotaExhaustedError`. The seat both of them point at has been unregistered since brw-16. |
| 2026-08-31 | notify-01 | New `autoloop/notify.py`, plus `[notify]` in `autoloop/config.py` and `config.example.toml`: the loop emails the operator from inside `heartbeat.publish` — the one chokepoint every status update already passes, so a new call site cannot forget — on a CHANGE of (status, phase, task id, decision), persisted beside the heartbeat so a restart does not re-send. OFF by default. It reports only what the loop KNOWS (park, block, pause, clean stop, task/phase transitions) and CANNOT report a hung, crashed or killed loop: that loop sends nothing, exactly as it writes no heartbeat. Only `scripts/autoloop_health_notify.sh`, outside the process, sees silence. |
| 2026-08-31 | notify-01 | Containment for the above, because `publish` runs in the hot loop and is documented as unable to raise: every failure is swallowed and logged to stderr, ONE attempt is made on a daemon thread joined for `notify.timeout_seconds` (<= 60) and never retried, and the change is RECORDED BEFORE the attempt — so a dead SMTP server costs one timeout per status change and nothing per beat. An in-process cache backs the persisted record, so an unwritable state file cannot turn "mail on a change" into "mail on every beat", and at most 3 abandoned sends may be outstanding. |
| 2026-08-31 | conc-01 | `docs/AUTOLOOP.md`: the concurrency split plan — nine dependency-ordered candidates, each independently reviewable, with the setting first and the rebase-aware merge second because it is the only piece provable without an N>1 fixture. CORRECTS the brief's obstacle #1: port-01 and esc-02 moved the loop's writes out of the observed tree, so the real collision is `ObservedCheckout.synchronize` rewriting a shared clone inside another lane's window. Boundary = one observed checkout per lane; the new residual is a write into a SIBLING lane's tree, detected but attributed to that lane. |
| 2026-08-31 | conc-01 | The plan's two answers the brief demanded explicitly. OVERLAPPING SCOPES: correctness is the merge protocol's, not the scheduler's — a candidate whose base moved is carried forward and re-reviewed, so `_dispatch_task_push`'s existing sha/tree/packet checks refuse the old approval with no new mechanism; admission control gates only same-FILE declared paths, since `effective_approved_paths` unions six universal trackers into every task and gating on those would co-schedule nothing. N=1: zero test edits, enforced per candidate. `checkout_escape_detected` stays fleet-fatal. |
| 2026-09-01 | policy-01 | `autoloop/orchestrator.py`: `_step_executing` clears `state.policy_denials` AFTER the dispatch and only when the dispatch did not refuse — comparing the counter against its pre-dispatch value — instead of clearing it the moment `authorize_directive` allowed the directive. Every denial raised INSIDE `_dispatch` (the retired legacy git path, `_dispatch_recut`, `_dispatch_executor`, the push-time checks) therefore spent a counter that had just been zeroed, so `max_policy_denials` bounded none of them: 71 consecutive `legacy_git_path_retired` refusals in 14 minutes on 2026-08-31. The gate denial above it always counted. |
| 2026-09-01 | postcommit-01 | `autoloop/orchestrator.py`: the `legacy_git_path_retired` refusal of an unbound `push` was a FIXED POINT — `policy_denied_payload` carries none of a candidate's identifiers, so the next request bound to nothing either and the same approval landed back on the same branch. A `push` with a live, renderable candidate now RE-PRESENTS its postcommit packet (`_prepare_candidate_representation`/`_present_candidate_again`), so the next identical reply publishes; the push is still refused and charged. With nothing to approve (notify-01's published state) the refusal drops the clause naming a `reviewed`-stamped approval, which no outstanding request could supply. |
| 2026-09-01 | halt-04 | `autoloop/blockers.py` gains `REFUSED_WORK_RECOVERIES` — seven codes whose fault is that the loop REFUSED a round's work, answered under autonomous mode by `RECOVER_BY_REVISING`: re-dispatch as a `revise` carrying the refusal text as feedback. `refusal_identity` digests one refusal's (code, question, detail) and `Blocker.revised_refusals` stores it — that pair is the repeat guard's memory. `autoloop/orchestrator.py` queues the revise in `_queue_autonomous_revise` and dispatches it from `_step`, never from inside the park handler. Default off; `[autonomy]` gains no key. |
| 2026-09-01 | halt-04 | The bound, since a resend without one is worse than the park: one revise per task per `refusal_identity`, spent by `BlockerStore.note_refusal_revise` when the revise is queued and counted back by `refusal_revises` across every record on disk, closed ones included, so no closure refunds an allowance. The revise deliberately does NOT arm `_autonomous_recovered_blocker` — a round completing means the round ran, not that the refusal cleared, so closing the record there would refund the meter every round. Existing locks compose unchanged: `_revise_feedback_is_unchanged`, `max_review_rounds`, `MAX_TASK_ATTEMPTS`. |
| 2026-09-01 | halt-04 | Keyed on the REFUSAL rather than on its code because a code is not a complaint: a DIFFERENT complaint under the same code gets its own revise — `post_commit_verification_failed` covers five checks — and refusing it because a sibling fault spent the code's allowance would park work that had an answer. What that leaves unbounded HERE, a code whose text churns, is bounded by `MAX_TASK_ATTEMPTS`/`max_review_rounds`, both of which end in a set-aside that policy refuses to revise. The changeset arms of `push_not_descendant`/`push_tree_mismatch` name no task and park exactly as today. |
| 2026-09-01 | intake-01 | `autoloop/inbox.py` gains audit-finding intake: ONE parser (`parse_audit_findings`/`newest_audit_report`) matching `audit/report._finding_block`, and a durable ledger at `<intake_dir>/audit_intake.json` recording each finding as PROMOTED, ALREADY DONE or DECLINED. `autoloop/dashboard.py` deletes its own narrower `_FINDING`/`_SEVERITY`/`_is_safe_glob` and reads through that parser, so the id an outcome is keyed on IS the id the panel filters on; `app_tasks` now returns OUTSTANDING findings and `audit_findings_panel` ships the counts beside them. `python -m autoloop intake audit list/promote/done/decline`. |
| 2026-09-01 | intake-01 | The fail-closed halves. An UNREADABLE ledger is not "nothing recorded": it filters nothing, says so on the page, and refuses the write rather than overwriting every decision in the file. ALREADY DONE is never inferred — `verify_already_done` re-reads each `path` / `path:text` / `path:!text` check off the tree, and a missing file refuses even the negated form, where a deleted file would otherwise satisfy "the bug is gone". `promotion_spec` REFUSES a spec with <2 approved paths or no validation instead of filing the rt-09/rt-13/rt-14 shape; `[repo].app_test_root` and `app_validation` are the two new settings it reads. |
| 2026-09-01 | intake-01 | `TaskInbox.pending_creation_ids` reads the ids QUEUED creation requests already claim, and `promote_finding` refuses a spec whose id one of them holds — before it writes either the request or the ledger. `tasks.json` cannot see an undrained request, so two findings deriving one task id (`db-01` and `db_01` both give `audit-db-migrations-db-01`) previously queued two requests for it: the drain applies the first, `add_many` refuses the second, and both ledger entries claim a runnable task. The refusal names `--task-id`; a file the reader cannot parse is skipped because `drain` rejects it too. |
| 2026-09-01 | select-04 | `autoloop/validation.py`: `_scan_module` ATTRIBUTES the two signals it used to read off source text. A dynamic import whose module name is a CONSTANT this checkout does not own (`__import__("socket")`, `pytest.importorskip("asyncpg")`) can reach no repository code; an interpreter literal or `sys.executable` counts only once a `subprocess` entry point starts something. Every uncertainty keeps the old answer — a non-constant name, a call whose first argument is not a module name, an argv this cannot read (`["git", *args]`), a spawn reference that escapes uncalled, a file that will not parse. `ImportGraph.opaque` is still seeded on every change. |
| 2026-09-01 | select-04 | Measured before -> after on this checkout: opaque test files 20 -> 12, the floor under every selection 22 -> 16, a docs-only round 24/102 -> 20/102. `subprocess` is matched through the names the file BOUND, not by name alone, because `orch.run(max_steps=1)` shares one; `os.execv`/`os.system` are NOT watched, which the literal-only rule covered by accident, and a real-repository guard in `test_test_selection.py` fails the round that introduces one. |
| 2026-09-01 | select-04 | The brief's classification does not reproduce here and is corrected rather than implemented. It named 17 opaque files: `test_browser_provider_removed.py` carries `python3` only inside a docstring and was never opaque, and it omits four that are (`test_audit_executor`, `test_audit_intake`, `test_per_test_selection`, `test_transport_vocabulary`). Three it called false positives spawn under its own rule and stay opaque: `test_crash_safety.py:390` runs pytest on this repository, `test_implement_executor.py:41` and `test_self_upgrade.py:207` hand `run_git`'s `*args` to `subprocess.run`. |
| 2026-09-01 | wanted-01 | `autoloop/contract.py` asks `wanted_decision` on EVERY reply — `(required)`, with the new `NO_WANTED_DECISION` (`none`) meaning "the verb I used is the verb I wanted" — instead of only "when none above fits", a condition that never held: measured 2026-08-25 the field had ZERO uses and `wanted_decisions.json` had never been created. `autoloop/policy.py` gains `_check_wanted_decision`, denying `wanted_decision_missing` for every decision, behind the retirement gate and ahead of every configurable one. PROTOCOL_VERSION stays 3 and the parser still accepts an omitted key. |
| 2026-09-01 | wanted-01 | The layering is `Decomposition`'s, for a reason specific to this field: a missing value raised in the PARSER is a `parse_error`, which spends the two-round parse budget and parks the loop `parse_budget_exhausted` — an outage over a bookkeeping field — where a policy denial re-prompts, costs no attempt and is capped by `max_policy_denials`. A whitespace-only value now parses as ABSENCE for that reason, and policy strips before testing so it does not depend on the parser doing so. `autoloop/orchestrator.py`'s two self-issued revise directives carry the answer rather than being exempted. |
| 2026-09-01 | wanted-01 | The value is still NEVER acted on: `_dispatch` branches on `decision` alone, nothing converts the string to a `Decision`, and `test_wanted_decision.py` AST-walks `orchestrator.py` so a third self-issued directive cannot skip the gate. `_record_wanted_decision` is unchanged and now actually runs — `none` is counted like any other value, before authorization, so a round denied for some other reason still contributes its answer. `docs/SCHEMA.md` gains the `wanted_decisions.json` row and `docs/AUTOLOOP.md` a "Which verb the reviewer wanted" section. |
| 2026-09-01 | prov-01 | New `autoloop/codex/reply.py`: `codex exec` stdout is a rendered transcript, not a reply — a separator rule, a `user` marker with the echoed prompt, hook lines, a `codex` marker with the answer, `tokens used`, and the answer AGAIN as the closing summary. `codex/conversation.py:372` handed all of it to `contract.parse_response`, which answered `invalid_json: the reply is not exactly one JSON value`, so `codex_cli` could not return a verdict at all. `isolate_reply` hands the contract the lines after the `codex` marker up to the first furniture line; the contract itself is untouched. |
| 2026-09-01 | prov-01 | The trailing duplicate and the echoed prompt are excluded STRUCTURALLY — the first sits after `tokens used`, the second under the `user` marker — never by de-duplicating and never by position, which is the rule `contract._extract_envelope` refuses. Two messages are refused rather than chosen between. |
| 2026-09-01 | prov-01 | Stdout with no isolatable message is a FAILED invocation — REJECTED plus a `codex_invocation_failed` note naming which way — never a defaulted decision. Undecorated stdout passes through unchanged, unless it is furniture and nothing else, which is refused here rather than by the parser: a parse refusal spends `max_parse_retries` and parks the loop. A counts-only `codex_reply_isolated` record makes the boundary visible. |
| 2026-09-01 | prov-01 | The echo needs more than the segment rule: this loop maintains itself, so its packets QUOTE codex transcripts and a `codex` marker plus a directive arrive inside the prompt. Three bounds — a marker starts at column 0, a marker opens a turn only after furniture, and the echoed prompt is skipped outright when found verbatim (`submit` passes the final prompt; `echo_anchor` records what the anchor did). Every marker inside the echo is a REFUSAL, never a fallback: an echoed example carries this round's own stamp, so the downstream head-SHA gate would accept it. |
| 2026-09-01 | prov-01 | A fourth bound closes the round the anchor cannot reach: a REFLOWED echo defeats an exact match, so a packet quoting a flush-left transcript, in a round whose reviewer message is empty, left one segment and it was ours. `reply.py:474` `_accept` is now the ONE place a reply leaves the module, and it refuses any candidate the prompt contains, literally or on `quota._squeeze`'s basis. Ambiguous with a reviewer that copied the packet's example, and resolved CLOSED: one resend costs less than an approval nobody made. |
| 2026-09-01 | prov-02 | New `autoloop/codex/preflight.py`: the ONE definition of what `codex.working_dir` means. Empty no longer means the HOME directory — codex refuses to run there (`Not inside a trusted directory and --skip-git-repo-check was not specified`, measured 2026-08-17), so the shipped default could not work while every doctor row about it said `ok`. It now means `~/.autoloop/codex-workdir`, a dedicated empty directory created on first use; a CONFIGURED directory that is missing is refused, never created, so a typo cannot become the place reviews run. |
| 2026-09-01 | prov-02 | `SubprocessCodexRunner`, `SubprocessAppServer` and `doctor` all resolve the working directory through that one function, so the path doctor grades is the path the reviewer gets. Both runners also ensure it before spawning: `subprocess` raises the same `FileNotFoundError` for a missing cwd as for a missing binary, and the CLI runner used to report both as "the codex CLI was not found". That message no longer advises setting `conversation.provider` back to the provider retired in brw-16. |
| 2026-09-01 | prov-02 | `autoloop/doctor.py` gains `codex_preflight`: one trivial invocation, from the configured working directory, with the configured `sandbox_args`, graded on its EXIT CODE — never on its own words appearing in the output, since `codex exec` echoes the prompt back. Injectable via `DoctorProbes.codex_preflight`, because the default launches a real process. `codex_workdir` now grades the resolved path; unusable means the preflight is `fail` ("not attempted"), never `skip`. |
| 2026-09-01 | prov-02 | `codex.sandbox_args` stays EMPTY, with the expired justification replaced by a stated policy: confinement rests on `working_dir` alone — a dedicated directory outside the checkout plus a self-contained prompt — because no flag name is verifiable from this repository or in CI. `config.example.toml` and doctor's warn row name `--sandbox read-only` and `--skip-git-repo-check` as the two an operator may add; whatever is set is passed to the preflight, so a rejected flag fails a check instead of a review. |
| 2026-09-01 | prov-02 | `docs/AUTOLOOP.md` gains "Selecting the codex reviewer": the ceiling neither transport raises — codex signed in with a ChatGPT account (`stored API key: false`, `auth mode: chatgpt`, verified 2026-08-17) draws on the SAME allowance the retired browser seat used, so switching transport removed the DOM faults and nothing about the account limit that parked the loop for four hours on 2026-08-17 — plus the confinement policy and what each of the four codex doctor rows means. |
| 2026-09-01 | prov-02 | New `autoloop/codex/sandbox.py`: `codex.sandbox_args` is read as a POLICY and now ships `["--sandbox", "read-only"]`. The earlier note above is superseded — it shipped that key EMPTY and called `codex.working_dir` the confinement, which is wrong: `cwd` selects where a process STARTS and restricts no absolute path, parent traversal, subprocess or read. Grading is fail-closed: no mode, an unknown mode, a dangling `--sandbox` and a bypass flag all `fail`, `workspace-write` warns, and where several modes are named the WEAKEST decides, since nothing here can run the binary to learn clap's precedence. |
| 2026-09-01 | prov-02 | An unconfined seat is REFUSED rather than warned about: `SubprocessCodexRunner.run` raises before spawning (the last point before the process), `preflight_codex` returns `fail` with no invocation, and `doctor`'s `codex_sandbox` fails and gates `codex_preflight` to "not attempted". The claim is narrow and stated as such everywhere it appears: the flags are PRESENT in the argv `subprocess.run` receives, and the preflight proves the configured build ACCEPTS them. Not claimed: enforcement, or that read-only confines READS — it does not. The policy is graded over `command` AND `sandbox_args`, since codex sees one argv. |
| 2026-09-01 | prov-02 | Correction, superseding the read-only sentence in the notes above: `--sandbox read-only` restricts WRITES; it does NOT refuse command execution — codex runs commands inside the sandbox — and it does not confine reads. `sandbox.py`, `conversation.py`, `config.py`, `config.example.toml` and `docs/AUTOLOOP.md` all said otherwise and now say only what is verifiable, network behaviour included (unverified here, so unclaimed). No grading, status, refusal or default moved: `read-only` is still the shipped policy and still `ok`. |
| 2026-09-01 | prov-02 | Which working directory is AUTOLOOP'S now travels from the setting (`preflight.working_dir_is_default`) instead of being inferred from the resolved path. `ensure_working_dir` compared the path against the default and created it on a match, so `codex.working_dir = "~/.autoloop/codex-workdir"` — the documented default, copied into a config, not yet created — provisioned itself like an unset one: the single configured path the "a typo is never created for you" rule missed. The parameter defaults to REFUSING, creation needs the flag and the path to agree, and `SubprocessCodexRunner`, `SubprocessAppServer` and `doctor` all carry it. |
| 2026-09-01 | conc-02 | `autoloop/config.py` gains `[concurrency].lanes` (default 1, ceiling `MAX_LANES` = 8) and the lane vocabulary `LANE_ID_PREFIX` / `lane_id(index)` — candidate 1 of the nine in docs/AUTOLOOP.md's split plan. NOTHING reads the setting yet: at 1 an absent section and an explicit `lanes = 1` load to EQUAL configs, and the field is appended after `notify` so no positional construction moves. 0, negatives, floats, strings, `true` and anything above the ceiling are refused naming `concurrency.lanes`, never clamped. A lane id starts with `_`, which `validate_task_id` refuses, so it can never name a `workers_root` entry. |
| 2026-09-01 | conc-02 | Correction, superseding the ceiling in the note above: `MAX_LANES` is FOUR, not eight. docs/AUTOLOOP.md's "What this plan does not decide" hands this pick to conc-02 and asks it to say how, so the constant's comment now does: four is the largest fleet the plan itself describes (Decisions 1 and 6, and the measured 4x), it is NOT a measurement, and conc-10 is where a measured one would replace it. `config.example.toml` reads 1..4; every refusal interpolates the constant. |
| 2026-09-01 | conc-05 | `autoloop/state.py` gains `lane_paths` / `lane_state_file` — candidate 4 of docs/AUTOLOOP.md's split plan. Lane 0 resolves to LITERALLY `state_dir/state.json`, today's path, and only lanes above 0 get `lanes/<lane_id>/`; that asymmetry is the `lanes = 1` criterion made structural, so no reader moves and no new file appears. The index is validated BEFORE the lane-0 branch, since `False == 0` would otherwise hand a bool the one path that must not move. `config.lane_id` is imported inside the function because `state` -> `config` -> `policy` -> `tasks` -> `state` is a real cycle today. |
| 2026-09-01 | conc-05 | `autoloop/lock.py` gains `LaneLease` / `LaneLeaseInfo`: `<lane_id>.lease.json` beside a lane's state file, judged live by `LoopLock.is_live` ITSELF rather than a second copy of it. `LoopLock` is untouched — same path, same adoption, `unlock` still refuses a live fleet lock. Acquisition is one `O_CREAT` + `O_EXCL` create and nothing else; a DEAD lease is refused rather than taken over, because read-then-overwrite lets two processes both conclude a dead lane is theirs, and recovery is conc-08's, from inside the fleet lock (`break_stale`). An unreadable lease refuses instead of reading as a free lane. |
| 2026-09-01 | conc-05 | Nothing acquires a lane lease yet, deliberately: held for `cli._cmd_run`'s lifetime it would be a live lease naming this pid when `os.execv` replaced the process for a self-upgrade, and the successor would fail closed — the fleet lock has a token handoff for that and a lease has none. conc-06's supervisor takes one per lane and reaches the boundary by draining. `cli._load_state` now resolves through the lane resolver (lane 0 = today's path, same bytes) and `Orchestrator` takes a validated `lane_index`, defaulted to 0 and last in the signature, so no construction site moves. |
| 2026-09-02 | conc-05 | Correction, superseding "nothing acquires a lane lease yet" above: `cli._LaneEntry` takes lane 0's lease inside `_cmd_run`, around the lane's work and inside the `try` whose `finally` publishes the heartbeat, so every exit that unwinds releases it — and takes NOTHING at `lanes = 1`, which is what keeps "no new file under the state dir" true by construction rather than by a flag. The one exit that does not unwind is `os.execv`: the lease is released right after the lock is armed, the handoff is REFUSED (`exec_failed`, lock disarmed) when it will not go, and an exec that raises re-enters the lane or fails closed. |
| 2026-09-02 | conc-05 | `LaneLease.acquire` now removes the lease it has just created when the write or the fsync fails, and renders the record BEFORE the `O_EXCL` so an unencodable one strands nothing. It is the single unlink in that class that does not read the record first, and it is safe for the reason no other one is: `O_EXCL` has just proved this call created the file, so there is no incumbent to steal from — while leaving it closes a lane forever over an ENOSPC with no process in it. The raise still propagates: a caller that could not write its lease is not in the lane. |
| 2026-09-02 | conc-05 | Refining the note above: `LaneLease.acquire` writes the record in a LOOP, not one `os.write`. A short write returns a count and raises nothing, so a single call could leave a TRUNCATED lease its owner believed it held, which every later `read` then refuses — a lane closed with no process in it. A call returning 0 raises `OSError`, deliberately that type so the cleanup above unlinks the file `O_EXCL` just proved it created; retrying would hang the lane rather than refuse it. Ownership is claimed only after the last byte and the fsync. |
| 2026-09-02 | conc-06 | `orchestrator.FleetSupervisor.plan` is a pure function of the registry, the busy lanes (`lane_occupants`, read from each lane's own state file) and whether an upgrade is pending: it admits at most one READY task per free lane in `TaskRegistry.ready_in_dispatch_order` — the ordering `next_ready` now returns the head of, so the urgent pin keeps one implementation — holds the rest with a reason, and writes nothing, so a held task stays `pending` with no attempt charged. An unreadable lane counts as busy and stops admission rather than raising the cap. |
| 2026-09-02 | conc-06 | `cli._round_boundary_may_upgrade`: a CONTINUOUS run above one lane no longer builds an orchestrator that may end a ROUND for a self-upgrade — the replacement has to happen with the whole fleet idle, and a per-round boundary would exec while a neighbouring lane was mid-round. The single-round path keeps it whatever the fleet size: `_run_locked` has no supervisor and can never reach a drain, so switching it off there would leave a merged upgrade with nothing able to act on it, and deferring out loud (`UPGRADE_DEFERRED`) is the outcome that has to survive. |
| 2026-09-02 | conc-06 | The admission gate is `tasks.co_schedule_conflict`: DECLARED `approved_paths` only, file entries only, the six universal trackers and every directory entry dropped, so two tasks sharing `autoloop/tests/` and the trackers co-schedule and two naming one file do not. A pending upgrade drains — no admissions — and `cli._run_continuous` REACHES the boundary once every lane is empty rather than sleeping beside the record; a refused handoff declines the sha and admission resumes. `cli._fleet_plan` answers None at `lanes = 1`, where nothing consults any of it. |
| 2026-09-02 | conc-06 | Correction, superseding "every directory entry dropped" above: the gate drops the six trackers and `tasks.CO_SCHEDULE_EXEMPT_PATHS` (`autoloop/tests/` alone, matched exactly) and gates every OTHER declared entry, directory or file — dropping everything that ends in a slash co-scheduled two tasks both declaring `autoloop/`, two scopes each authorized to write every file the other one touches. `declared_file_entries` is renamed `declared_scope_entries` for it. Containment (`autoloop/` beside `autoloop/cli.py`) stays a residual Decision 3's merge protocol owns; that section is corrected to match. |
| 2026-09-02 | conc-06 | The plan now governs DISPATCH too, closing the residual Decision 3 left to conc-10: `Orchestrator._refused_outside_fleet_admission` asks the same `FleetSupervisor.plan` about the task a directive names (`plan(first=...)`, so the answer is about the fleet and not about queue position) and refuses a held one through `_handle_policy_denial`, before `mark_in_progress` — so the task stays `pending`, unwritten and unattempted. Inert at `lanes <= 1`, silent about a `revise` of the arc this lane owns, and fail-closed when the fleet's state cannot be read. |
| 2026-09-02 | conc-06 | Correcting "inert at `lanes <= 1`" above for one lane it must not be inert for: a lane whose index is at or above `[concurrency] lanes` is NOT excluded from its own occupant list, because `lane_occupants` walks `range(lanes)` and never reaches it — so a lane an operator's lowered cap cut out was invisible, the plan counted a free slot that no longer exists, and it admitted. `_refused_outside_fleet_admission` refuses it explicitly (`HOLD_LANE_RETIRED`) before it plans and before that return, since 4 -> 1 cuts out the same lanes 4 -> 2 does. It reads no file, lane 0 at one lane is untouched, and the arc a cut lane holds still finishes. |
| 2026-09-02 | conc-06 | The MIRROR of the note above, and the fail-open half of it: a lane the lowered cap cut out is still running, so `orchestrator.retired_lane_occupants` lists `state_dir/lanes/` for live sessions at indices `lane_occupants` no longer walks, and `fleet_occupants` — the union — is what `cli._fleet_plan` and the dispatch gate now read. Without it the fleet counted free slots it did not have and, worse, read `fleet_idle` as True and reached a self-upgrade boundary with a cut lane mid-round. An unlistable `lanes/` holds everything; a missing one is no cut lane, because lane 0 can never be one. |
| 2026-09-02 | conc-11 | `autoloop/state.py` gains `FleetThrottleStore` / `fleet_throttle.json`, beside the fleet lock: ONE `retry_not_before` and ONE consecutive-episode counter for the whole fleet. conc-05 gave every lane its own state file, so `LoopState.rate_limit_backoffs` became N counters against one ChatGPT allowance — at four lanes 4 x 6 = 24 throttled attempts to discover a limit one lane finds in 6. A lane meeting the limit inside an open window JOINS the episode (counter unmoved, deadline unextended, it waits out the REMAINDER); one meeting it afterwards opens `previous + 1`, so the escalation is still 60/120/240. |
| 2026-09-02 | conc-11 | The read-decide-write of joining is one hold of `tasks.task_file_mutex` — done outside one, two lanes both read an expired deadline and both open episode 1, the counting bug rebuilt inside its own fix. `policy.max_rate_limit_backoffs` is checked against the SHARED number by a JOINER as much as by the opener, or the opener parks at six while three lanes wait forever on a stopped fleet. `FleetSupervisor.plan` holds every READY task `fleet_rate_limited` while the window is open, and holds — never passes — when the record cannot be read; a lane already mid-round is untouched, it is ADMISSION that stops. |
| 2026-09-02 | conc-11 | `[concurrency] rate_limit_release_jitter_seconds` (5s, refused above 60) spreads the release: lane k re-probes `k/lanes` of it after the shared deadline — deterministic, so `health` can state it, and strictly less than the value. At `lanes = 1` it is 0.0, no record is written or read, and the delay sequence, the persisted stamp and the park text are today's. Streak inheritance is bounded by one previous delay, or a fleet parked at episode six would poison the next run's first throttle hours later. `health.check` carries a `fleet_throttle` view on every verdict. |
| 2026-09-02 | conc-11 | An unreadable record PARKS the lane (`fleet_throttle_unreadable`) rather than degrading to its own counter: `_handle_rate_limited` runs inside `run`'s `except RateLimitedError:`, where a raise is caught by no sibling clause including the `StateError` one, and the fallback would put the fleet back on N independent back-offs at the moment its shared record is in trouble. `_refused_outside_fleet_admission` deliberately keeps NO throttle store — that gate charges `max_policy_denials`, and a window clearing in seconds must not park a round it was never about. |
| 2026-09-02 | conc-11 | Correcting one line of the notes above: `_handle_rate_limited` reads the clock for the persisted stamp AT THE PERSIST SITE at one lane, exactly where it always has, and reuses the earlier episode read only on a fleet — where `delay` IS the remainder measured against that instant, so a second read would count the elapsed time twice and push the lane past the window it just joined. RESIDUAL, not fixable inside this task's paths: `cli._run_continuous` sleeps only when draining or when the queue is non-empty and wholly held, so an EMPTY queue falls through and opens an audit session while the fleet is throttled. |
| 2026-09-02 | conc-11 | SCOPE REQUEST for that residual, recorded so it is not rediscovered: closing it is one term — `cli._run_continuous`'s `plan.draining or (plan.held and not plan.admitted)` becoming `plan.draining or plan.fleet_throttled or (plan.held and not plan.admitted)` — plus a regression test that a throttled fleet with an empty queue opens no audit session and still logs the hold. It needs `autoloop/cli.py` in the round's approved paths. An in-scope gate inside `Orchestrator.run` was rejected: `Phase.READY` recurs mid-round, so gating on it stalls lanes this task says must finish or park. |
| 2026-09-02 | conc-11 | That request was granted and the residual is closed: `cli._run_continuous` now holds on `plan.draining or plan.fleet_throttled or (plan.held and not plan.admitted)`, so a throttled fleet with an EMPTY queue no longer falls through to `_select_and_kickoff` and opens an audit session against the allowance it is waiting out — nor does it hold silently, since `_log_fleet_hold` is on the branch it now takes. The term is the plan's own answer on that same tick, never a second read of the record, and is never set at `lanes = 1`, where `_fleet_plan` answers `None` before a supervisor exists. |
| 2026-09-02 | conc-11 | Ending an episode is now a COMPARE-and-clear: `FleetThrottle.episode_id` names one episode, `LoopState.fleet_throttle_episode` mirrors the one a lane observed, and `FleetThrottleStore.clear(episode_id)` unlinks only while the two still match — under the mutex `observe` takes. The unconditional unlink let a lane that waited out episode n delete episode n+1, opened by another lane between its retry and its completed step, erasing a live deadline and an escalated counter. `backoffs` is not that identity: a fresh streak is episode 1 again. An empty id, and a record that cannot be read, clear nothing. |
| 2026-09-03 | conc-03b | `autoloop/cli.py`: at `lanes > 1` a candidate bound to the head stops holding the merge window and starts owing a RE-REVIEW — a note, plus a `MergeObligation` appended to the list a caller passes (`auto_merge.AutoMerger.attempt` is the one that does). Candidate 2 of docs/AUTOLOOP.md's split plan, redone against the current head after conc-03's own patch stopped applying. `BASE_UNVERIFIED`, the executing-phase reason and every exemption above it are untouched at every lane count; at `lanes = 1` the reason string is byte-identical and nothing is ever appended. |
| 2026-09-03 | conc-03b | `autoloop/auto_merge.py` marks each bound candidate `rereview_owed_base` BEFORE the merge and carries it forward after, through an injected `carry_forward` (`orchestrator._carry_candidate_past_for_merge`). The order is the claim: a carry-forward that conflicts leaves the reviewed sha and tree UNCHANGED, so every push-time check would pass and the approval taken against the moved base would publish. The marker is what refuses it. A missing `carry_forward` (the startup sweep) DEFERS rather than merging; a record that cannot be marked defers too. |
| 2026-09-03 | conc-03b | `autoloop/orchestrator.py`: the merge-driven carry-forward ADVANCES `candidate_sha` to the merge commit — unlike `_rebase_execution_if_stale`'s use of the same machinery, where a new round's own commit advances it a moment later — so the old `PostcommitBinding` no longer matches and `_dispatch_task_push` refuses it (`push_rereview_owed`, `task_fatal`, ahead of `push_candidate_stale`). `review_round` is reset as the plan asks; the rounds move to `carried_review_rounds`, which the round cap and the stale-record guard both read, so no budget is refilled and no guard switches off. |
| 2026-09-03 | conc-03b | `autoloop/merge_sweep.py` passes `carry_forward` through to the merger it builds and re-evaluates nothing itself: the obligation is minted by the per-branch gate call `AutoMerger.attempt` already makes, so a three-branch sweep marks and carries three times, each onto the head the merge before it left. The all-or-nothing property is unchanged — the sweep-wide gate still defers everything or nothing, a merge that cannot discharge its obligations defers, and a carry-forward that refuses parks that task without touching the sweep's outcome. |
| 2026-09-03 | conc-03b | `autoloop/orchestrator.py`: a successful carry-forward now ends in the loop ASKING for the re-review instead of a human being asked to relay one. `_ask_for_the_owed_rereview` renders a fresh packet for the carried candidate where the OWNING LANE meets the obligation — holding the approval the moved base just invalidated — and discharges the marker only there, so the push still publishes nothing. Five fail-closed refusals keep the park: no task in the registry, no carried candidate, an outstanding stat-only split ask, a spent round budget, a packet that will not render. Each says which in the transcript and in the park. |
| 2026-09-03 | conc-03b | That ask is licensed by `TaskExecution.rereview_candidate_sha`, written by the carry-forward in the same save as the candidate it advanced — deliberately NOT inferred at push time from "the base moved and the approval names a different sha", since both are equally true of an approval stale for an unrelated reason, and inferring would route that one to a re-review instead of the `push_candidate_stale` refusal it has always taken. The round cap is one expression again (`_review_rounds_exhausted`), asked by the ask and by the dispatch that opens a revision round. |
| 2026-09-03 | conc-04b | `config.lane_observed_checkout` is the per-lane clone rule (candidate 3 of the split plan, redone against the current head): lane 0 at `lanes <= 1` is EXACTLY `[paths].observed_checkout`, and every other (lane, fleet) pair is `<observed>/<lane_id>`. The asymmetry is on the FLEET SIZE, not the index as in `state.lane_paths`, because a clone inside another lane's clone puts that lane's whole working tree in this one's snapshot window — the misattribution rebuilt inside its own fix. A lane at or above the cap still gets its own tree, so a session an operator's lowered cap cut out never lands on lane 0's. |
| 2026-09-03 | conc-04b | `worker_env.validate_observed_checkout` gains `other_lane_checkouts` (empty by default, so every existing caller keeps today's answer): a lane's clone may not BE, be nested beneath, or CONTAIN another's, and a non-absolute sibling is refused rather than resolved against the caller's cwd. `ObservedCheckout.for_lane` returns SELF at one lane and carries `runner` forward above it. `Orchestrator.__init__` narrows the wired checkout to its own lane's after `lane_id` has validated the index, so the snapshots, the clean baseline and `_worker_fetch_root` follow one tree per lane. `escape_detector` is unchanged: docstring only. |
| 2026-09-03 | conc-04b | `worker_env.validate_lane_checkout_distinctness` is that sibling rule extracted so it can have a PRODUCTION CALLER, which it lacked: `Orchestrator._lane_isolation_violations` derives every other lane's clone from the deployment path and fleet size recorded at construction, and `_synchronise_observed_checkout` asks it BEFORE the sync — a violation parks `observed_checkout_unusable` with nothing fetched, checked out or run. Both sides are RESOLVED, so `_lane-0 -> _lane-1` is refused where the derivation cannot see it, and a check that cannot answer refuses. No siblings at one lane, so nothing is compared there. |
| 2026-09-03 | ctx-03 | New `autoloop/context_records.py` (the record: `source_paths` asserted AT `last_verified_commit`, plus `related_ids`, `superseded_by`, `invariant`; unknown keys and unmatchable paths refused at parse time, a bad file reported by name rather than dropped) and `autoloop/context_index.py` (one load, indexes by id and by source path; two files declaring one id are indexed under NEITHER and reported, so no record silently wins an id). Shape only — where records live is ctx-04's, and the resolver takes its directory as an argument. |
| 2026-09-03 | ctx-03 | New `autoloop/context_resolver.py`: seeds plus the transitive closure of explicit `related_ids` (no similarity search, no embeddings), ordered kind-then-id, one recorded reason per selection, and missing / superseded / dangling-supersession / contradictory / stale / budget-dropped each reported as its own category. Staleness is a TRI-STATE by tree comparison — one `diff-tree` per distinct commit, and a commit that will not resolve is `unknown`, never fresh. New `[context] max_records` in config and the template bounds it and names every drop. No git primitive and no allowlist change. |
| 2026-09-03 | conc-07 | `blockers.LANE_FATAL_CODES` / `FLEET_FATAL_CODES` / `fatal_scope` are candidate 6 of the split plan: how far one `loop_fatal` park reaches, on a SEPARATE AXIS from `kind` — no park site's kind changed and `state.park_kind` still carries literally `loop_fatal`, keeping every existing assertion true. 13 codes are lane-fatal (the lane's own session, conversation, request, candidate, record or clone); the rest are fleet-fatal in four families, and an absent, empty, unknown or non-string code is too — with the hard halts refused ahead of the table, so `checkout_escape_detected` cannot be moved by a table edit. `Blocker.lane_id` names the lane that parked. |
| 2026-09-03 | conc-07 | `orchestrator.fleet_stop` is how a lane learns a sibling reached a fleet-fatal terminal: it walks the lanes' own state files (in-cap, then the ones a lowered cap cut out, via the extracted `_retired_lane_indices`) and reads a `needs_user` park or a `stop_kind="fault"` stop, never the blocker set — a record is written BEFORE `_to_needs_user` decides to park, so a recovered fault would otherwise stop four lanes. `task_fatal` stops nothing; an unreadable lane state, an unlistable `lanes/`, a missing kind and an unestablished code all stop the fleet. `cli._run_continuous` asks once per outer iteration, after `pause`/`abort`, and only at `lanes > 1`. |
| 2026-09-03 | conc-07 | `cli._log_fleet_stop` writes one `fleet_stop` transcript entry on the tick a lane gives up for somebody else's fault — `_log_fleet_hold`'s sibling and for its reason: the durable evidence lives in `blockers/` and belongs to the lane that PARKED, so a lane that only printed a line to a terminal would leave no record that it stopped or whom it stopped for. It clears when that lane's own session leaves `needs_user` (`run --retry`/`--answer` there, or `reset`); answering the blocker record alone does not move it, and the printed remedy says so. |
| 2026-09-03 | conc-08 | `orchestrator.recover_dead_lanes` is candidate 7 of the split plan: a lane whose LEASE is dead beside a mid-round state file is RESUMED — the lease removed, so the next tick re-enters and the saved phase continues — or QUARANTINED first when the worker its record names is on disk and fails `worker_repo_is_reusable`. Only that lane's lease, state, clone and worker are named, which is why no other lane can be touched, and the worker is dealt with BEFORE the lease, so a quarantine that failed leaves the lane shut. Called from `Orchestrator.run`'s startup block, excluding its own lane, never raising, only at `lanes > 1`. |
| 2026-09-03 | conc-08 | The dead LEASE is the evidence and the state file is not: a mid-round session with no lease beside it is what an ordinary `run` leaves between rounds, and recovering it would quarantine a worker an operator is about to resume. Every absence refuses instead — an unreadable lease, an unreadable state, an unknown phase, an unreadable execution record, an unlistable `lanes/`, a quarantine that fails — and a refusal leaves the lease where it is, so nothing enters that lane. Recovery is only safe from the FLEET-LOCK holder (`LaneLease.break_stale`'s check-then-act); one process holds it today and conc-10 inherits that. |
| 2026-09-03 | conc-08 | `merge_sweep.MergeToken` (`merge_token.json`, beside the fleet lock) serialises merging above one lane: `BacklogSweeper.sweep` takes it after the window gate and returns it in a `finally`, and a token a live lane holds DEFERS this sweep rather than being stolen. A dead holder's is released by `recover_dead_lanes`, judged on the TOKEN's own record and never on a lease — one corrupt lease must not leave the fleet unable to merge. `health.dead_lane_survey` is the one predicate both readers share; `Health.dead_lanes` reports it without changing the code or `needs_attention`. At `lanes = 1` no token exists and no file is written. |
| 2026-09-03 | conc-08 | `orchestrator._lane_clone_violations` / `_park_dead_lane` land Decision 8's third bullet in the recovery itself: before a dead lane is reopened, its OWN clone (`ObservedCheckout.for_lane`, so an injected runner survives) is asked the read-only half of `synchronize`'s refusals — present but not a repository, and residue, including residue nobody could read. A violation writes the park a live lane writes (`needs_user`, `loop_fatal`, `observed_checkout_unusable` naming the directory), which `fatal_scope` keeps lane-fatal, so the fleet runs on. Nothing there is fetched, reset or deleted: the tree is the evidence. |
| 2026-09-03 | conc-08 | The blocker record is written BEFORE the state park and the park abandoned if it cannot be — `_lane_fleet_stop` resolves a park's SCOPE through `park_blocker_id` and an unresolvable code is fleet-fatal, so a park with no record behind it would stop every lane over one lane's dirty clone. A clone that could not be established at all refuses and touches nothing; an absent or empty clone, a terminal lane and a deployment wiring none are not judged on one; AHEAD/DIVERGED stays with `_synchronise_observed_checkout`, which refuses it with the identical park at the next dispatch. |
| 2026-09-03 | conc-08 | `orchestrator._deployment_observed_checkout` now refuses on ANY exception rather than on a three-name tuple. It is the one call `recover_dead_lanes` makes OUTSIDE its per-lane `try`, so an escaping error skipped `_release_dead_merge_token` entirely — one unbuildable path leaving the whole fleet unable to merge, which is the coupling Decision 8 states the token must not have. The answer is unchanged (`None`: inspect no clone) and is not a fail-open: `_synchronise_observed_checkout` refuses the same trees, lane-fatally, before any write-capable agent runs. |
| 2026-09-03 | conc-08 | The quarantine moves the path the execution record NAMES (`WorkerRepoManager.quarantine_recorded`), never `path_for(task_id)`: every judgement is made about `execution.worktree_path`, while `quarantine(task_id, …)` moves `<workers_root>/<task_id>` as configured now — so a moved `workers_root` left the broken worker in place and carried a healthy namesake off instead. A recorded path that cannot be placed is refused: `_recorded_worker_refusal` keeps it out of the state dir and the observed-checkout root, both directions, and the manager requires a real directory, never a symlink, named for the task and holding none of its own roots. |
| 2026-09-03 | conc-08 | It quarantines that path only where the record and today's `workers_root` name ONE directory, established before the move by `_recreated_worker_refusal`: a worker that fails the reuse probe is RECREATED by the next dispatch, which builds at `path_for(task_id)` and then works in `execution.worktree_path` without writing the record back. Where those differ the dispatch refuses (the namesake) or fills a directory the round never opens, so the mismatch REFUSES — nothing moved, the lease kept, both paths named every tick — rather than reporting a recovery that strands the lane. |
| 2026-09-03 | conc-08 | Revision round: no source changed. The claim was re-verified against the committed implementation: the `lanes > 1` gate on `Orchestrator._recover_dead_lanes`, the park that stops re-entry, the sibling clone's `.git/index` inside the byte-identity digest, and the live merge token never being taken. `ruff` and the full suite pass with no existing test edited. The patch is the committed diff on this branch: the review round's requested attachment does not exist in the worker repo and was not manufactured, because a hand-written patch is the agent's account, not the diff. |
| 2026-09-03 | conc-08 | `_lane_clone_violations` asked distinctness of `range(lanes)` alone, so a RETIRED dead lane — the survey walks those too — was compared against the in-cap lanes and against nothing else: two retired clones resolving to one tree passed, and `residue()` then ran `git status` inside the OTHER retired lane's repository and refreshed its `.git/index`, one lane's recovery writing into another. `_sibling_lane_checkouts` now unions `_retired_lane_indices` in, and an unlistable `lanes/` refuses that lane rather than reading it as having no siblings. |
| 2026-09-08 | conc-09 | `autoloop/health.py`: `check` grows a per-lane pass above `[concurrency] lanes = 1`. `fleet_health` judges every lane on its own state file and its own lease (`LaneHealth`, codes from `VERDICT_CODES`, `busy` borrowed from `orchestrator._lane_occupant`), and `_judge` reads NO lane's state above one lane — so `Health.phase` is empty and no lane's phase is presented as the fleet's. The fleet verdict is the most severe lane needing a person, never lane 0's by position, and `FleetHealth.at_cap`/`idle` keep a fleet at its cap apart from an idle one. `Health.fleet` is `None` and OMITTED from `to_json` at one lane, where the output is unchanged. |
| 2026-09-08 | conc-09 | `autoloop/dashboard.py`: `lanes_status` / `render_lanes_text` / `lanes_json` render every lane over `health`'s own rows rather than a second reading of the same files, kept out of `collect` exactly as `projects_status` is, lock-free — the loop holds `LoopLock` for its whole run, so a panel that waited for it could only render a stopped fleet. `_current_task` now answers for the FLEET above one lane (`_fleet_task`: `3 lanes: brw-19 +2`, the oldest in-flight task and how many others) and never prints lane 0's task, or lane 0's phase, as the system's. |
| 2026-09-08 | conc-09 | Revision round. `health._strand_survey` read only lane 0's `state.json`, so above one lane a task lane 1 had just been dispatched met all four strand conditions and was reported stranded while an agent worked on it. `_lane_claims` now gathers every other lane's `(task_id, age)` — in-cap and retired — and `stranded_fault_rounds` takes them as keyword-only `lane_claims`, each judged against `round_ceiling_for`'s bound SEPARATELY. A lane whose state cannot be read, or a `lanes/` nobody can list, abandons the survey with a note that escalates rather than sweeping without it. Gated at `lanes <= 1` before any read. |
| 2026-09-08 | conc-09 | Same round: the lanes panel gains its call site — `autoloop lanes` (`cli._cmd_lanes`) prints `dashboard.lanes_status` through `render_lanes_text` / `lanes_json`, read-only and lock-free, exit 0 fine / 1 a lane needs you, and 0 at one lane. A command rather than an HTTP route for `_cmd_projects`' reason: the dashboard's front door is bound to a CHECKOUT while `[concurrency] lanes` lives only in a config, so a route would have had to invent config discovery inside the server. `orchestrator._reconcile_stranded_tasks` keeps its lane-0 reading — it ACTS — and is named in docs/AUTOLOOP.md for conc-10. |
| 2026-09-08 | conc-10 | `autoloop/cli.py`: `_run_fleet` turns concurrency on. Above one lane `run --continuous` opens N lanes as N threads in ONE process holding the one `LoopLock` — one process because `LaneLease.break_stale`, `MergeToken`'s recovery and `recover_dead_lanes` are all check-then-act sequences safe only from the fleet-lock holder. `_run_continuous` is lane-aware: `_load_state(config, lane_index)`, and `_build_orchestrator(..., lane_index=...)` passed only when non-zero so every six-argument test double still matches. Nothing here is reached at `lanes = 1`. |
| 2026-09-08 | conc-10 | Same round, the ownership boundary: `_lane_owns_upgrade` / `UPGRADE_OWNER_LANE` gate BOTH doors to the self-upgrade boundary on lane 0. `FleetPlan.upgrade_boundary` is a fact about the FLEET, so every lane saw it and the first to arrive could fail to hand off and decline the sha into `answered_upgrades` — after which `_drainable_upgrade_sha` answers `""`, the drain stops and the owner never sees the upgrade again. A non-owner now declines into a lane-local set (`_decline_boundary_not_ours`, `self_upgrade_not_this_lane`) and lands on the drain's own hold. |
| 2026-09-08 | conc-10 | Same round: the owner does not `os.execv` inside a lane thread either — the replacement keeps the pid, so a sibling lease still on disk would name the successor's own pid, read as live, and fail it closed on its own lane. `_FleetRun` stops every lane, each unwinds its own `_LaneEntry`, and `_run_fleet` reaches the boundary with lane 0's entry the only one left, restarting the stopped lanes if it returns. `_cmd_run` calls `recover_dead_lanes` once above one lane before entering lane 0, since a killed fleet's own lane-0 lease would otherwise stop the next run before it started. |
| 2026-09-08 | conc-10b | `autoloop/tasks.py`: the registry lost update, which had to close before lanes could be raised. Every lane holds its registry for a whole round and every save writes the whole thing, so a lane recording its own transition wrote a stale copy of its neighbours' rows back — a dispatch, completion or quarantine undone with nothing said. `TaskRegistry` now remembers the rows it loaded/last saved (`rebaseline`, `changed_since_baseline`) and `TaskStore.reconcile_concurrent_rows` adopts, WHOLE, every disk row this registry did not change, plus rows only the file has (tasks another lane added). A row this registry changed is never overwritten. |
| 2026-09-08 | conc-10b | Same round: that reconciliation runs FIRST inside `save`'s existing mutex hold, before `reconcile_priorities` — which mutates the row, so running it first would make an operator-reprioritised task read as this lane's own change and skip its neighbour's status silently. Gated by `TaskStore(fleet=...)`, set once from `[concurrency] lanes > 1` in `cli._load_tasks`: an in-memory value with no file to fail open on, and at one lane no store reconciles anything but priority. A hand-built registry has no baseline, so it adopts nothing whoever calls it. Fails open on an unparseable file, as the priority pass already did. |
| 2026-09-08 | conc-10b | Same round: `orchestrator._claim_task_for_dispatch` makes the claim atomic — reconcile, refuse, `mark_in_progress` and save inside ONE `tasks.task_file_mutex` hold. `mark_in_progress` could not be the check: it is idempotent on an already in-progress row, so a second lane marking a task the first is running succeeds silently. The refusal is narrow on purpose — only a row THIS hold adopted, i.e. one that moved while this registry sat in memory — reported as `HOLD_IN_FLIGHT` on the ordinary corrective re-prompt with the task untouched and unattempted. At one lane it reads nothing and refuses nothing. |
| 2026-09-08 | conc-10b | Same round: `autoloop/publisher.py` serialises publication. `publisher.git` is one repository every lane pushes through, and two concurrent `import_candidate` fetches collide on git's own `FETCH_HEAD` lock — git fails the second, parking a lane over a neighbour's timing. `publisher_mutex` wraps `import_candidate` and `publish` on the task file's own primitive, with a timeout from `worker_env.OBSERVED_GIT_TIMEOUT_SECONDS` rather than its 10s, since the holder is inside a network push. A wait that expires leaves as `GitCommandError`, never `TaskStoreBusy`, so it parks like any push failure. The lock file is pre-created at provisioning. |
| 2026-09-08 | conc-10b | Revision round, docs only: no code changed. The ownership gate this candidate was recut for is in the tree and green (`cli._lane_owns_upgrade` / `UPGRADE_OWNER_LANE` on both doors), so this round's work was an adversarial pass over it. It found one shared-record writer that is NOT gated: `cli._confirm_self_upgrade` retires the `execed` one-shot from every lane, so one replacement can be confirmed once per lane, and a load/clear window lets a sibling's merge write a `pending` record the unlink then removes. Named in docs/AUTOLOOP.md rather than fixed — closing it is a compare-and-clear mutex, not a gate. |
| 2026-09-08 | conc-10b | Same round, the second residual the adversarial pass found and docs/AUTOLOOP.md now names: `_run_fleet` does not restart a lane that ENDED, so a LANE-fatal park in lane 0 leaves the fleet with no upgrade owner, and an upgrade merged after it drains forever — every remaining lane is a non-owner and lands on the drain's hold. Loud rather than silent (lane 0's blocker record, `health` red, a `fleet_hold` entry per poll) and cleared by a restart. Fleet-fatal parks never reach it. Closing it is a scheduler decision — ownership follows the lowest LIVE lane, or the runner restarts a parked owner — not this gate. |
| 2026-09-08 | conc-10b | Both of those residuals are now CLOSED. `auto_merge.UpgradeStore` serialises every WRITE to `pending_upgrade.json` (`_hold`, borrowing `tasks.task_file_mutex`; only the acquisition converts, to `UpgradeRecordBusy` — an `OSError` subclass, because every caller on this path already answers a failed write with `except OSError` and a `StateError` past a boundary would leave by traceback), and `clear(base_sha=…, status=…)` is a compare-and-clear inside that hold. Gate: `UpgradeStore.for_config`, `lanes > 1` read once from memory; at one lane no mutex file appears beside the record. |
| 2026-09-08 | conc-10b | Same round: `cli._confirm_self_upgrade` therefore removes the record it READ and never the newer `pending` one a sibling's merge wrote in the window between its load and its clear — which also makes the confirmation once per REPLACEMENT rather than once per lane, since the first lane's clear matches and the rest do not. A refusal writes `self_upgrade_confirm_skipped` rather than nothing. `_carry_on_upgrade`'s own removal of the marker it wrote takes the same comparison, so cleaning up after a refused handoff cannot delete a sibling's fresh upgrade. |
| 2026-09-08 | conc-10b | Same round: the upgrade owner follows the LOWEST LANE STILL RUNNING (`cli._FleetRun.upgrade_owner`), so a lane-fatal park in lane 0 no longer leaves a live fleet with no owner. `_open_lanes` declares the pass before any thread starts and `_lane_thread` records a departure in a `finally`, so a lane not yet started never reads as one that finished; exactly one lane owns the boundary at any instant and the answer only moves UP. No lane running means no owner — fail-closed. Still decided in memory, never from a file. `UPGRADE_OWNER_LANE` is now where ownership STARTS. |
| 2026-09-08 | conc-10b | Revision round, tests only: no source changed. An adversarial pass over the ownership gate found both doors still gated (`cli.py` — the `SELF_UPGRADE` outcome and the drain), `_reach_upgrade_boundary` the only writer of `answered_upgrades`, and no path reaching `_run_continuous` above one lane with `lane.fleet` unset: `_cmd_run` gets there only through `_run_fleet`, which joins lane 0 to the fleet before a thread starts. What was missing was the ORDER on the `SELF_UPGRADE` door — proved for the non-owner alone — so `test_fleet_end_to_end.py` now drives the owner arriving after it on the same record. |
| 2026-09-08 | ctx-04 | `Task.context_ids`: the context records a task says it was written from, and PROVENANCE only — neither `tasks.effective_approved_paths` nor `unauthorized_paths` reads it, so citing one widens no byte of scope. One validator (`_validate_context_ids`) is shared by creation, `TaskRegistry.set_context_ids` and the load; `_persisted_context_ids` normalises a hand-edited `null` and refuses a bare string, so `TASKS_SCHEMA_VERSION` stays 1 with no migration step. Reachable as inbox `KIND_CONTEXT_IDS` (refused in flight by `_refuse_immutable`, like `approved_paths`), `add-task --context-id`, and one ids-only line in `context.TaskBrief`. |
| 2026-09-09 | ctx-05 | New `autoloop/context_packet.py`: every implement/revise round gets a CONTEXT PACKET rendered from its own worker repository at `TaskExecution.task_base_sha` — the cited ids, each selected record's source paths WITH their blob object ids at that commit, the reason per item, the stale/superseded/contradictory records and the unresolved questions — hashed into the new `TaskExecution.context_packet_sha256` and stored at `AutoloopConfig.context_packets_dir` (state dir, beside `executions/`, never in the checkout). |
| 2026-09-09 | ctx-05 | Same round: `orchestrator._dispatch_task_postcommit` renders that packet below every path that can still move the base and above the agent, so a revise round after a rebase is cut from the NEW base; `packet.build_review_packet` carries the same digest and the packet's own bytes, after every identifier line, and WITHHOLDS text that does not hash to it. The record decides whether the section exists, so an audit packet is byte-identical to before. `implement_executor` gains the fail-closed `context_packet_for` reader; wiring it in `cli._build_executor` is out of this round's scope, so the PROMPT half is dark in production. |
| 2026-09-09 | ctx-05 | Revision round: the prompt half is LIT. `orchestrator._dispatch_task_postcommit` hands the rendered section to the executor immediately before the call and clears it in a `finally` (`implement_executor.deliver_round_context_packet`), rather than through a constructor keyword — production's executor is `cli._DispatchingExecutor`, which forwards only `execute`, so no keyword or attribute could reach the object that builds the prompt. The same dispatch refuses a round whose packet cannot be stored and read back at its recorded digest. |
| 2026-09-09 | conc-12 | DEFECT 1's cause, measured: `orchestrator._reconcile_stranded_tasks` ran at the top of every `_step_ready` with only THIS lane's claim, so a neighbour's live round met all four of `health.stranded_fault_rounds`' conditions and was RELEASED to `pending` with its agent still working — which is why the row read `pending`, `next_ready()` kept returning it, and the in-flight backstop became the primary gate. It now passes every other lane's claim (`health.fleet_lane_claims`, `_lane_claims` generalised to exclude the ASKING lane) and HOLDS the sweep, saying so, when a lane cannot be read. |
| 2026-09-09 | conc-12 | Beside it, the selector: `TaskRegistry.set_lane_view(held_elsewhere, prefer)` narrows what `next_ready()` OFFERS without narrowing `ready_in_dispatch_order`, which the supervisor must still classify (a task with no hold reason is a task the dispatch gate lets through). `Orchestrator._refresh_lane_view` builds it each `_step_ready` from the neighbours' adopted rows and their own state files, fails SOFT with a transcript entry, and carries a hold's named alternative into the next offer. Empty at `lanes = 1`, before any read. |
| 2026-09-09 | conc-12 | And DEFECT 2: `already_in_flight`, `scope_conflict` and `fleet_at_cap` (`FLEET_HOLDS_NOT_CHARGED`, matched on the HOLD WORD carried beside the reason, never on the sentence) spend `state.fleet_holds` rather than `policy_denials`, so a hold can no longer reach `policy_denial_budget_exhausted` — loop_fatal, and it stopped every lane three times in ninety minutes. `MAX_FLEET_HOLDS` bounds them into a CLEAN stop (`FLEET_HOLD_STOP_KIND`), and `_end_refusal_streak` watches both counters so a hold cannot zero a denial streak. |
| 2026-09-09 | ctx-06 | Planning may not assert a repository fact nobody read. `inbox` gains `Claim`, `SOURCE_PRECEDENCE`, `EVIDENCE_READERS` (an allowlist of READERS — a path is a location, not a reader), `unsupported_claims` and `detect_conflicts`, all built on the existing `Evidence`; `audit/findings.Finding` gains five OPTIONAL keys (`current_behaviour` and its citation are a PAIR) plus `claims()`; `audit/taskgen.generate_tasks` refuses an uncited finding BY NAME into `skipped`, records a source disagreement through `blockers.record_planning_conflict`, and STOPS rather than choosing when one bears on scope. |
| 2026-09-09 | ctx-06 | Corrections to the line above. The unit of disagreement is the AUTHOR, not the tier (`inbox.Claim.author`): two accepted tasks scoping one finding differently now conflict instead of being suppressed for sharing `accepted_decision`. The finding id is matched at a word boundary (`taskgen._mentions`), so `d1:f1` no longer fires on `d1:f11`. And a conflict with no durable record stops generation and says so in `skipped`, which the report renders. Wiring a real `BlockerStore` and the operator's request into `audit/executor.py:803` is outside these approved paths and is reported, not made. |
| 2026-09-09 | ctx-06 | Round 2, first half: a citation is CHECKED, not merely path-shaped. `inbox.TreeReader` reads `git ls-files`, and `claim_problem` — which now holds every rule `unsupported_claims` reports — refuses a CURRENT-BEHAVIOUR claim whose audit-report citation names a path the tree does not have, and refuses one no reader was available to check at all, in different words ("nothing was read" is not "not there"). Bounded to model-authored readers and to claims about current state, so a finding proposing a file it will CREATE is not called a fabrication. |
| 2026-09-09 | ctx-06 | Round 2, second half: `cli._build_executor` attaches `inbox.PlanningSources` — a real `BlockerStore`, a LAZY `TreeReader`, the operator's intake drafts as an operator-request source, and a note naming the context tier nothing reads — to the registry `audit/executor.py:803` already passes, so record-a-conflict-and-continue is the shipping behaviour rather than a branch nothing reaches. A `sources` claim is validated too: refused into `skipped`, still compared and still recorded verbatim, and never restated in a task description (`inbox.describe_conflict_for_task`). |
| 2026-09-09 | ctx-07 | CONTEXT CLOSEOUT: at completion `orchestrator._close_out_context` (immediately after `_mark_task_completed`) classifies the published change against the records THAT ROUND'S PACKET SELECTED — re-resolved and confirmed against the packet's own bytes by `context_packet.selection_was_shown` — advances `last_verified_commit` on a touched `feature`/`incident` record whose own file is inside the task's `approved_paths`, and names everything else in ONE inbox follow-up depending on the completed task. New `context_records.ContextRecordStore` writes a record; `superseded_record` retires one without rewriting it. |
| 2026-09-09 | ctx-07 | Same round: question three is never answered by the loop — a decision's successor is a claim somebody authors, so a touched decision is always a follow-up line — and question four only on ctx-02's bar, measured off `TaskExecution.attempt_ledger`: the same failure outcome more than once, so a clean round creates no lesson. Nothing on the path reads the agent's report or the reviewer's feedback. `Orchestrator(context_records=…)` is the ONE wiring point and feeds both the packet's index and the closeout, so the two cannot disagree; `cli` names no directory, so production logs `context_closeout_skipped`. |
| 2026-09-09 | ctx-07 | Same round: the scope question is `tasks.unauthorized_paths` over `effective_approved_paths` on the record's OWN repository path, so a context write outside scope meets the same matcher the pre-commit gate and the post-commit ownership check share, and the choice is binary — write it in scope or file the follow-up. Nothing there can reach `set_approved_paths`, the inbox's approved-paths kind or `TRACKER_PATHS`. New `tasks.is_valid_approved_path` / `is_valid_context_id` answer off the existing validators, so one odd record id cannot get the whole follow-up refused on drain. |
| 2026-09-09 | ctx-07 | Same round, the trap the adversarial pass found: a record store wired INSIDE the observed checkout would leave a file the loop cannot commit, and the next write-capable dispatch parks the whole loop `primary_checkout_dirty` over it. `_store_is_inside_the_observed_checkout` refuses the closeout before any write and logs it — port-01's "nothing is written inside the checkout" reaching a new writer, fail-closed on a path it cannot resolve. So the directory lives outside the tree and `repo_prefix` says what its files are CALLED in it, which is all the scope check reads. |
| 2026-09-09 | ctx-08 | CONTEXT EXPLAIN: `autoloop context explain --task <id> [--packet]` prints, for one task's round, which records were selected and why, which were rejected and why, which are stale or contradictory, and three digests — rendered now, on the execution record, in the stored packet file — each said to agree or not. Read-only and lock-free like `blockers`: no writes, and only `rev-parse`/`ls-tree`/`diff-tree`, so it is usable mid-round. |
| 2026-09-09 | ctx-08 | Same round: new `context_packet.PacketRender` and `render_packet_with_resolution` are the ONE render the dispatch and the diagnostic both read, so the two cannot disagree; `render_context_packet` is now a reader of it, and the record sections are the packet's own `selection_block` bytes rather than a second spelling. `context_resolver` gains `REJECTED_CATEGORIES` and `STALENESS_CATEGORIES`, grouped beside the constants they name. |
| 2026-09-09 | ctx-08 | Same round: no silent caps — the bounds section names what the command withheld (the packet's text, sized, behind `--packet`) and separately what the RESOLVER's budget dropped, each dropped record being named above it. It explains the round the execution record NAMES and refuses rather than inventing a base: an unknown task, a missing or corrupt record, and a worker path that is not a directory each exit 1 with the reason — `Path("")` is `Path(".")`, which would have rendered a packet from the operator's own checkout. A differing digest is exit 0 and names its innocent causes. |
| 2026-09-09 | ctx-08 | Revision: `context explain` no longer presents a present-time resolution as the round's selection. The ANCHOR is `TaskExecution.context_packet_sha256`, and `context_packet.provenance_verdict` answers AS DISPATCHED (a re-render reproducing it), RECORDED PACKET ONLY (the stored packet hashes to it — printed verbatim and FIRST, with the re-resolution demoted to a labelled comparison) or UNVERIFIED (neither, or no digest at all: an empty one matches nothing). A worker repository that has moved is now answered from the recorded packet rather than refused, and no git runs for it. |
| 2026-09-10 | split-06 | SPLIT-ORDER ADVISORY: `validation.split_order_warnings` reads a split plan against the graph `build_import_graph` already computes and names every part that holds a Python module whose importer sits in a part not reachable from it — the brw-19 shape. `tasks.paths_within_scope` is the positive complement of `unauthorized_paths`, so ownership is decided by the matcher the scope gate enforces rather than by a second one. `orchestrator._apply_split` carries the result into the transcript, the reviewer's report and every successor's brief. |
| 2026-09-10 | split-06 | It WARNS and never refuses — a scope cannot tell a deletion from an edit, so refusing would gate a plan on evidence that does not distinguish them — and it FAILS OPEN, unlike the rest of `validation.py`: an unbuildable or truncated graph, an unreadable checkout or part, or any unexpected exception accepts the plan unchanged and records `split_order_ran: false` with a reason. See `docs/SCHEMA.md` §Split-order advisory for the transcript keys and both arguments. |
| 2026-09-10 | split-06 | Revision: `_apply_split` appends ANY non-empty advisory to every successor's brief rather than only a flagged one. `SplitOrderReport.describe()` is empty only when the check RAN and found nothing, so a clean plan still leaves each brief byte-identical, but a `ran=False` — which by construction names no edge — now reaches the briefs as well as the transcript and the reviewer's report. The agent it matters to is the one whose part cannot succeed inside its own scope: it must be able to read that nobody checked the order rather than spend its attempts concluding the fault is its own. |
| 2026-09-10 | conc-14 | CARRY-FORWARD PAST THE FILES EVERY TASK TOUCHES: `note_merge.resolve_counter_bump` combines two branches that each raised the same hand-written counter for the test file they added. Merged value is `base + (ours-base) + (theirs-base)`, read from index stages 1/2/3 and never from the half-merged working file, with both sides' classification comments kept in `lead` order. `combine_conflicted_notes` routes each conflicted path through `resolver_for`; both callers gain the shape, since they share that function. |
| 2026-09-10 | conc-14 | Same round: `SUITE_SIZE` was declared once per selection test, so two tasks each adding a test file conflicted in four files and the two trackers' resolution was refused with the two copies — every carry-forward at `lanes = 2` parked. New `autoloop/tests/suite_size.py` is its one home, still hardcoded: a side that raises the count without appending a classification line is REFUSED, not summed, so a new test file is still classified on purpose. All-or-nothing resolution is unchanged and argued in `note_merge`'s docstring. |
| 2026-09-10 | review-01b | IMPOSSIBLE SCOPE: an `ASSUMPTION:` line reporting that the task cannot be done inside its approved paths now stops the next `revise` instead of buying another round. `packet.reports_impossible_scope` requires BOTH halves in ONE collected line — the approved paths named as the wall AND an explicit statement that the work cannot be done — so a file a round left alone on purpose is not read as a blocker. `orchestrator._revise_cannot_help` then parks `approved_scope_blocks_task` (`task_fatal`, task-scoped); the park quotes the disclosure verbatim and names the three answers that can move the task. |
| 2026-09-10 | review-01b | Same round, the placement: the guard runs FIRST in `_dispatch_task_postcommit`, above the attempt ceiling, the fault ceiling and the review-round cap — each of which ends such a task under a code naming a budget, and on brw-19a the attempt ceiling is what got there first. New `Orchestrator._reviews_delivered` is the meter, `review_round + carried_review_rounds`, shared with `_review_rounds_exhausted`: charged once per packet and never once per dispatch, so a round the loop had to redo is not counted twice. The review packet also flags a standing disclosure to the reviewer. |
| 2026-09-10 | review-01b | Revision: `IMPOSSIBLE_SCOPE_NOTICE` renders VERBATIM at the assumptions heading's own column instead of being re-indented into the list under it. It is the LOOP's finding rather than the executor's claim, and the column says so; emitting the constant's own bytes is also what lets a test or a grep pin it, the way `STAT_ONLY_PACKET_BANNER` and the `OUT-OF-SCOPE PATHS` literal already are. What decides the notice is unchanged and was never the defect: it is computed over the WHOLE record, so a disclosure the render budget drops still carries it. |
| 2026-09-10 | stream-01b | `audit/agents.py` gains the agent ACTION LOG: `ClaudeCliRunner(action_log_dir=)`, absent at every construction site today and therefore off, appends the agent process's own stdout/stderr to `<state_dir>/action-logs/<task>-<round>.log` from INSIDE the supervisor loop — once per tick, through the `sleep` seam `supervise` already takes. `os.pread`, never `seek`: `Popen(stdout=<file>)` hands the child a dup of that descriptor and dups share one offset, so a seeking pump moves the position the agent is about to write at. |
| 2026-09-10 | stream-01b | Same round: `[audit] action_log` (bool, default false, a non-bool refused rather than read as truthy) and `AutoloopConfig.action_log_dir` (`state_dir/action-logs`, outside the observed checkout) are the config half, and the template documents both. The log is capped at 8 MiB of agent output and says IN the file when it cut; an unopenable or unwritable log announces once on stderr and the round is unchanged. |
| 2026-09-10 | stream-01b | The production wiring is BLOCKED BY SCOPE, not declined: the write-capable runner is built by `implement_executor.implement_agent_runner` and called from `cli._build_executor`, and neither file is in this task's approved path list — the plan named `autoloop/executor.py`, which holds only the `TaskExecutor` protocol and `NullExecutor` and is on no execution path. Two lines close it: an `action_log_dir: Path | None = None` parameter forwarded by `implement_agent_runner`, and `action_log_dir=config.action_log_dir if config.audit.action_log else None` at the `agent_runner_factory` lambda. |
| 2026-09-10 | stream-01b | Because of that gap the key is documented as INERT in both places an operator reads it — `config.example.toml`'s `[audit]` block and the `AuditConfig.action_log` docstring — each naming the two lines that close it and asking for the paragraph to be deleted then. A key that reads as configured while doing nothing is the failure this repository already refuses to ship; announcing it is the difference between that and this. |
| 2026-09-10 | stream-01b | Revision: the setting now REACHES a round. `load_config`'s last act calls `audit.agents.set_default_action_log_dir` — the configured directory when `[audit] action_log` is on, `None` when off — and `ClaudeCliRunner` resolves that once in `__init__` when no directory of its own is named, so the runner `implement_agent_runner` builds for a real round streams without either that factory or `cli._build_executor` changing. The INERT paragraphs in `config.example.toml` and `AuditConfig.action_log` are gone: they described the round before this one. |
| 2026-09-10 | stream-01b | Why a process default rather than an argument: those two call sites are still outside this task's approved paths, so the value cannot be passed at them. It is applied in BOTH directions, so a `false` config disarms a process a `true` one armed; it is the LAST thing `load_config` does, so a config refused by any check arms nothing; and an explicit `action_log_dir=` still wins over it. Passing the argument at the two sites instead stays a strictly smaller change and nothing here forecloses it. |
| 2026-09-10 | stream-01b | Revision: the armed default is now BOUNDED. `audit.agents.INHERIT_ACTION_LOG_DIR` is the `action_log_dir=` default and the only value that consults it, so an explicit `None` means OFF again rather than "ask the process"; and only a runner with a `progress_probe` inherits, so the write-capable supervised runner a round runs streams while the read-only audit runners — `subprocess.run(capture_output=True)`, whose output exists only once the process has exited — are left as they were rather than handed a file that reads like a live stream. |
| 2026-09-10 | stream-01b | Same revision: the review asked for those two files to be authorized so the directory could be threaded through `cli._build_executor`'s per-task factory into `implement_agent_runner` and the process default deleted. They were not — this dispatch's approved paths are unchanged — so the default stays, bounded as above, and the two-line change that retires it is still the one recorded here. `test_the_cli_builds_the_production_runner_so_that_it_inherits` reads `cli.py` to pin that its per-task factory passes `policy=`, which is what gives that runner the probe the gate needs. |
| 2026-09-10 | stream-01b | Revision: the process-wide default is GONE. `cli._build_executor` computes `config.action_log_dir if config.audit.action_log else None` once and passes it to `implement_agent_runner`, which forwards it to `ClaudeCliRunner(action_log_dir=)`. `set_default_action_log_dir`, `default_action_log_dir`, `INHERIT_ACTION_LOG_DIR` and the `load_config` side effect are deleted, so loading a config changes nothing outside the object it returns and no runner depends on which config the process read last. |
| 2026-09-10 | stream-01b | Same revision: `autoloop/cli.py` and `autoloop/implement_executor.py` were edited although this dispatch's approved paths again omit them — the review named both verbatim as the fix. Both write-capable sites get the value, the standalone one included so they cannot disagree; the read-only audit runners are passed nothing and stay buffered and unlogged. Explicit `None` is off, and a value that cannot name a directory leaves the log off and announces rather than raising out of the runner's constructor. |
| 2026-09-09 | ops-01 | `autoloop/dashboard.py`: operator controls, at `/api/control`. Ten buttons, each ONE existing CLI verb — pause, abort, `run --continuous`, reset, release, discard, retire, answer, archive-blocker, merge-backlog — run in a subprocess, so every one takes `LoopLock` itself and applies its own refusals. This page writes nothing into a state directory, which is what keeps the escape detector able to attribute the write to the loop's own process. Adding a button added no authority. |
| 2026-09-09 | ops-01 | Same round, the sequence: arm the stop with the CLI's own `pause`/`abort`, WAIT for the lock to be released, then RE-READ the phase the loop actually landed in — the pause exit at the top of `Orchestrator.run` is NOT gated on the phase, so a stop armed in `executing` can land in `delivering`. An unsafe landing runs NOTHING and puts the loop back. The restart is `run --continuous`, never `resume` (one foreground round, then it exits), and is reported only against a LIVE lock under a NEW run id. |
| 2026-09-09 | ops-01 | Same round: `cli._merge_window_blockers` gains a `holders` out-param — `{task_id, reason}` per blocker, mirroring `obligations` — so `dashboard.merge_window` names WHICH task holds the window shut instead of parsing prose it printed itself. A holder with no task id (a phase executing, an unresolvable state dir) is reported rather than dropped: a SHUT window listing nobody reads as a bug in the panel. Every existing caller keeps the two-tuple it already unpacks. |
| 2026-09-09 | ops-01 | Same round, NOT done and split out rather than half-built: restoring an attempt budget after a fault (no CLI verb touches `attempt_count`, so there is no attested path to route through — the ledger is DISPLAYED instead, since that half can be done honestly today) and carrying a reviewed candidate past a moved head when the trackers conflict. Killing the in-flight agent and quarantining a BLOCKED task both HAVE verbs now (`abort`, `discard`) and are wired rather than deferred. |
| 2026-09-09 | ops-01 | Same round, the door every other gate left open: `reset --yes` against a STOPPED loop. Every other refusal in the panel is about stopping, so a loop already down in `awaiting` — a reviewer still holding its packet — was offered a control that archives that session. `OperatorAction.discards_session` is asked BEFORE the running test, so reset is refused whether or not there is anything to stop, and reset alone: a locked verb that touches no session is still the right thing to run against a loop that is down. |
| 2026-09-09 | ops-01 | Same round, the fleet: "may the loop be stopped" is asked of EVERY lane, not lane 0. One `LoopLock` stops all of them, so `dashboard._lane_sessions` walks `state_dir/lanes/` — the DIRECTORY, never `[concurrency] lanes`, because a lane a lowered cap cut out is still running — and any lane owing a packet refuses the stop, naming which. A lane with no `state.json` has never run and owes nothing; one that will not parse, and a `lanes/` nobody can list, both refuse. One `is_dir()` at a single-lane deployment. |
| 2026-09-09 | ops-01 | Same round, that fleet rule where it actually decides: the POST path's post-boundary re-read asked lane 0 alone, so a fleet that PASSED the pre-check could land with lane 1 mid-deposit and the verb ran anyway. `perform_control` now re-reads every lane at the boundary — fresh, never reused from the pre-check snapshot — so an unsafe LANDING in any lane runs nothing, names the lane and puts the loop back. Also `dashboard._pending_view`, which duck-types the nested `pending_request` so a refusal names the request id instead of `?`; which states refuse is unchanged. |
| 2026-09-10 | ops-01 | Same round: both readers of `state.json` normalise a NON-MAPPING to absent. A bare scalar is a valid JSON document, so `123` parsed and then raised on the first `.get` — in `collect` that is the read-only page down, and in `perform_control` it is a raise AFTER the loop was stopped, where the handler's generic 500 leaves it down instead of restarting it. Both now read absent, which refuses every stop and restarts. `_lane_sessions` also takes `state.LANES_DIRNAME` rather than a second spelling of `lanes`. |
| 2026-09-10 | ops-01 | Same round, the review's finding: `perform_control` rolled an unsafe landing back only `if unsafe and action.verb`, so `pause` and `abort` — whose whole effect IS the stop — were left down in `delivering` with a successful-stop report and a stranded packet. Every action that armed a stop is now rolled back and refused; `OperatorAction.restarts` is documented as governing the SUCCESS path only, and the refusal is worded per action by `_unsafe_landing_reason`, since "NOTHING was run" is false of a stop that landed and an abort's kill is not undone by the restart. |
| 2026-09-10 | ops-01 | Same round, the trap inside that wording: "the loop was put back" is what a rolled-back stop wants to end on, and in front of a `_restart` that FAILED it would sit directly above "THE LOOP IS DOWN" — two claims about one loop, one false. `_unsafe_landing_reason` takes the restart's own verdict and states only what was verified; the restart's message states the rest. |
| 2026-09-10 | ops-01 | Same round, the adversarial pass: `dashboard._restart` re-read the PAUSE flag after proving the lock live and never the ABORT one, though it clears both — and either flag stops a loop: `cli._run_continuous` asks `pause_requested` then `abort_requested` at the top of every outer iteration and returns on either, before it resumes a session or selects a task. A flag landing in that window meant "restarted and verified alive, pid N" about a loop ending at its first iteration. `_armed_stop_flag` now reads both through `CONTROL_STOP_FLAG_READERS`, with no `getattr` default, and a test asserts that table against every `OperatorAction.arm`. |
| 2026-09-10 | ops-01 | Same round, the review's residual defect: every gate in this panel was about STOPPING, so a loop ALREADY stopped in `awaiting` passed all of them, and Release then archived the execution record and quarantined the worker of the task whose packet the reviewer still held. |
| 2026-09-10 | ops-01 | Fix for that: `dashboard.session_holds` carries the task ids the saved session names into the control snapshot, and every `needs == "task"` control refuses THAT task while a packet is outstanding, running or not. Task-scoped, so another task's Release is unchanged; an unattributable packet refuses every task, and a MISSING state file is not a session. |
| 2026-09-10 | conc-13 | `cli._merge_window_blockers`' EXECUTING-phase clause becomes a per-candidate obligation above one lane, finishing the Decision 6 conversion conc-03 left half-done. It read `config.state_file` — LANE 0's — so it was unsound (one lane speaking for the fleet) and starving (lane 0 executes nearly always; measured 2026-09-09, two lanes for 5.5h, `autoloop/mainline` unmoved). Above one lane the predicate now reads NO lane's state file at all, which is Decision 7's rule and `health._judge`'s own device. At `lanes = 1` the read, the reason string and the `merge-window` OPEN line are byte-identical, and no existing test needed an edit. |
| 2026-09-10 | conc-13 | Same round, the safety that clause was buying, kept by machinery more precise than it was: a reviewed candidate bound to the head is the obligation (marked before the merge, carried after, refused at push time); a DIRTY worker — an agent literally mid-write — is precondition 4 of `_carry_reviewed_candidate_past`, which refuses rather than merging over the residue and parks `task_base_behind_head`; a round with no candidate yet is skipped and its worker is a separate clone no merge touches. `merge_sweep.py` and `auto_merge.py` needed no code change: conc-03's per-branch gate already re-mints obligations between merges. |
| 2026-09-10 | conc-13 | Same round, three prose claims corrected because the diff made them false: `health.py`'s held-sweep paragraph (a window "closed because a phase is executing clears in minutes" is true at one lane and was the STEADY state at N, so a reader could not tell a healthy fleet from one whose merges had stopped), `auto_merge.py`'s "no executing phase" gate summary, and `config.example.toml`'s copy of it. `_cmd_merge_window`'s OPEN line is lane-aware for the same reason: above one lane both of its clauses would be overclaims. |
| 2026-09-11 | conc-13 | Same round, the residual that correction must NOT be read as closing, stated in `health.py` and in Decision 6 rather than papered over: it removes a CAUSE of an invisible multi-lane stall, not the blind spot. `held_merge_sweep` ages on a task the sweep could not JUDGE, and `SWEEP_DEFERRED_EVENT` is in `merge_sweep.SWEEP_CLEARED_EVENTS` — so a sweep deferring for any other reason (a `BASE_UNVERIFIED` candidate, the merge token held by a sibling lane, a moved remote base) CLEARS that signal instead of ageing it and stays transcript-only. A backlog stalled on a deferral is still unreported; that is a follow-up. |
| 2026-09-11 | conc-13 | Same round: that clause was also buying SERIALISATION by accident, and `auto_merge.AutoMerger.after_completion` — the path EVERY lane takes when it publishes, into the ONE shared checkout — took no token. Opening the window made two completions two simultaneous merges (`index.lock`, a merge verified against a head the sibling moved, one lane's abort unwinding the other's). It now takes the SAME token `merge_sweep` takes, through one shared `merge_sweep.take_merge_token`, held across the whole drain because the window verdict, the marks and the verification are one sequence. `attempt` takes none: the sweep already holds it. |
| 2026-09-11 | conc-13 | Same round, the fail-open that pairing exists to close: the token gate read `merge_sweep._fleet_lanes` while the WINDOW reads `config.concurrency.lanes > 1`, and a window that opens as a fleet while the token is skipped as one lane is two lanes merging with nothing saying so. `merges_are_serialised` is now the union of both readings — a lane count that cannot be compared is a fleet, fail-closed. At `lanes = 1` no token object is built and no file appears under the state dir, asserted from inside the merge rather than after it. |
| 2026-09-11 | conc-13 | Same round, two hardenings the second caller made load-bearing. `MergeToken.acquire`'s "raced with a release" retry was an unbounded self-call, reached once per sweep before and now on every completion: it is `_ACQUIRE_RETRIES` attempts then `LockHeldError`, because a stack growing to `RecursionError` leaves by a path neither caller catches. And `take_merge_token` / `release_merge_token` refuse and swallow WIDELY rather than by exception type — both callers take the token outside their own `try/finally` and are documented never to raise, and refusing is the closed direction, so the worst outcome is a lane that does not merge. |
| 2026-09-11 | val-07 | `autoloop/implement_executor.py`, `autoloop/validation.py`: the agent's ADVISORY validation run is SELECTED at request time. `AdvisoryValidation.run` re-resolves its list through `ImplementExecutor._advisory_scope` — `git status` in the worker repo, minus the three rendezvous paths, through the same `_select_validation` the verdict run uses — so an agent iterating on one module runs that module's tests. MEASURED before: 40 rounds, 95 runs, each of them the complete resolved list. |
| 2026-09-11 | val-07 | Same round, FAIL CLOSED: a worker that is not a git repository, a status read that raises, a tree `validation.worker_tree_state` cannot digest, and any widened selection each run the CONFIGURED list whole — what every advisory run did before — and the answer names which happened, to how many test files, from which paths. A `--lf` rerun is never narrowed on top. The channel's own paths are subtracted because `PENDING` sits at the result path while the run is in flight, and its `.txt` tokens attribute nearly the whole suite. |
| 2026-09-11 | val-07 | Same round: the containment the brief leaned on (that run "is never WIDER than this one") is now CONDITIONAL and ENFORCED rather than dropped. Each run records the tree it selected from; `record_verdict_run` hands back the verdict run's own tree state and selection; `covers_verdict` grants coverage only to a full-list run, or to a narrowed one whose tree is byte-identical AND whose selected files contain the verdict's. An unknown state never compares equal to another. `note()` says covers / moved / never established. |
| 2026-09-11 | val-07 | Revision: the descriptor's `--lf` sentence is pinned by TWO tests pulling opposite ways — `test_the_description_warns_the_agent_before_it_ever_sees_a_rerun` requires the uppercase `NARROWER` (the val-08 warning, in the casing the brief shouts it in) and `test_the_description_never_promises_a_containment_that_does_not_hold` forbids `never wider`. The first draft lowercased it to "narrower still"; it now reads "NARROWER still than the run that grades you". Reword that clause with both tests open. |
| 2026-09-11 | ctx-16 | CONTEXT RECORDS NOW HAVE A HOME, and it is the TARGET REPOSITORY: new `[context] records_dir` (repository-relative, `docs/context` by default, `""` to wire nothing, every other unusable value refused at load rather than read as off) plus `context_records.repository_record_store` / `RepositoryContextRecordStore`, which `orchestrator._context_record_store` derives from the tree THIS LANE observes — one accessor for the dispatch's packet and the closeout that grades it. `context_closeout_skipped: no_context_record_store` no longer fires for an ordinary completed task. |
| 2026-09-11 | ctx-16 | Design A, and the write side IS the design: records are versioned and reviewed with the repository they describe, so `RepositoryContextRecordStore.write` refuses unconditionally — in the class, not only in the caller — and every record the closeout would have advanced joins the ONE narrow follow-up whose `approved_paths` are exactly those files, for a reviewed round to write. The observed checkout is read and never written; `_store_would_write_inside_the_observed_checkout` still refuses a WRITING store wired inside it, before any write, and nothing is excluded from the escape detector. |
| 2026-09-11 | ctx-16 | Same round, the limits stated rather than left to be found. Record CONTENT is read from the observed checkout while staleness is computed against the worker's `task_base_sha`, so a merge landing between a round's dispatch and its closeout that CHANGES the record directory draws `context_closeout_refused` naming exactly that. `updated: []` now has three readings, so a `notes` line names a deferral and a deferral never borrows the "the write failed" wording. `cli`'s `context explain` and the audit tier are still fed no index and now say why in those words. |
| 2026-09-11 | ctx-16 | Revision: `config._context_records_dir` accepted any whitespace-only value as the off switch via `not value.strip()`, while the docs, the example and the class all claimed only the literal `""` does. It now compares `value == ""`; a blank value falls through `clean_repo_prefix` to `""` and is REFUSED with the other unusable spellings, and the error gains "not blank" so the refusal names what was typed. |
| 2026-09-11 | ctx-16 | Revision 2, retiring the limit two lines up: repository records are now read OUT OF GIT AT `task_base_sha` through the worker's gateway (`context_records.load_records_at`, via `RepositoryContextRecordStore.load`), never off the observed working tree — so a packet's bytes are the bytes of the commit its provenance line names, even when the branch moved after the task was cut (wrk-01's reused stale base). `_context_record_index(worktree_git, base_sha)` and `plan_round_closeout` make the same `store.load` call, so the closeout cannot refuse for that store because the branch moved. |
